# Datastore Readiness And Completion Plan

Last reviewed: 2026-06-03

This document is the current product inventory for DataPad++ datastore support. It is meant to answer one question clearly: what still has to be finished before each datastore feels like a native, production-ready IDE inside DataPad++?

The evaluation is based on the current shared adapter backlog, completeness matrix, tree manifests, object-view descriptors, browser-preview explorers, and Rust adapter registry:

- `packages/shared-types/src/datastore-roadmap.ts`
- `packages/shared-types/src/datastore-completeness.ts`
- `packages/shared-types/src/datastore-tree-manifests.ts`
- `docs/architecture/native-completion-tracker.md`
- `apps/desktop/src/app/components/workbench/*ObjectViewDescriptors.ts`
- `apps/desktop/src/app/components/workbench/*ObjectViewWorkspace.tsx`
- `apps/desktop/src-tauri/src/adapters/registry.rs`

## Feature Complete Definition

A datastore is feature complete when it has all of these:

- native connection options, connection strings where appropriate, variables, secret safety, and friendly connection errors
- a live metadata tree that feels like the datastore's own tool, hides unavailable sections, and supports scoped refresh
- native query surfaces such as SQL editor, document builder, key browser, console, script mode, or graph/time-series builder
- metadata-aware IntelliSense from cached explorer/structure data, not per-keystroke metadata calls
- purpose-built object views for the engine's important concepts, without default raw payload dumps
- safe live edits for natural row/document/key/item changes only when the target identity is complete
- guarded admin/destructive operation previews with environment checks, read-only checks, and clear disabled reasons
- diagnostics and performance views such as explain/profile, sessions, locks, metrics, slow queries, cost, or capacity
- import/export or backup/restore workflows that match the datastore
- deterministic unit/browser-preview tests plus optional fixture/live tests when external dependencies are available

## Native Completion Tracker

The execution order, active datastore, per-engine native gaps, and acceptance gate are tracked in [Native Datastore Completion Tracker](native-completion-tracker.md). When a datastore completion slice starts or finishes, update that tracker in the same change as the shared completeness matrix and user-facing docs.

## Current Product Shape

DataPad++ now has a broad adapter foundation across 29 engines. Every declared engine is explicitly accepted for the contract-complete native UX gate in `packages/shared-types/src/datastore-completeness.ts`, with per-criterion contract evidence, a completion claim, and residual-risk wording that separates deterministic contract coverage from live production validation. Most engines have declared manifests, tree manifests, object-view descriptors, browser-preview payloads, operation manifests, diagnostics contracts, and test coverage. Secondary datastore connection flows now have shared/browser/Rust parity for typed option bags, right-drawer fields, browser validation, Rust interpolation, timeout selection, and secret-safe redaction; Memcached now has its own server-list/SASL/timeout option contract instead of relying only on generic host fields. Secondary datastore object views now use adapter-owned workflow descriptors that hide unavailable actions and jump to the relevant section instead of showing passive or generic chips. SQL-family object views expose guarded explain/profile/index/grants/export/backup previews through the shared operation-planning path, with dialect-aware browser and Rust previews, and SQL connections now have a visual table-and-relationship explorer for focused schema graph work. Core SQL now has live primary-key row editing across PostgreSQL-family, SQL Server, MySQL/MariaDB, SQLite, and TimescaleDB, with Oracle kept contract-only until a live driver path is added. MySQL and MariaDB now add Workbench-style security and diagnostics branches, compact storage/index/user/session posture panels, and guarded check/analyze/optimize/repair/event previews. Search-family object views now include cluster, field-capability, shard-health, Lucene segment, lifecycle, ingestion, and security posture panels, a profile result renderer, and guarded HTTP-shaped previews for explain/profile, force merge, cache clear, reindex, open/close, mapping/settings updates, alias work, data-stream rollover, template create/delete, pipeline update/simulate, lifecycle policy update, task cancel, security, bulk export, snapshot, and restore workflows. DynamoDB object views now include native key-design, capacity, TTL, stream, backup, hot-partition, and index-coverage panels plus guarded AWS-shaped previews for capacity, TTL, streams, backups, metrics, GSI changes, IAM-style access, import/export, and table deletion; its connection UI and contract now preserve typed local endpoint, AWS profile, static key, assume-role, web-identity, retry, timeout, and consumed-capacity options without storing secret values. Cassandra object views now include partition-model, storage/compaction, tombstone, diagnostics, and cluster-health panels plus guarded previews for tracing, index work, permission checks, diagnostics, imports/exports, and destructive table/object plans; its connection UI and contract now preserve contact points, secure bundles, TLS files, local datacenter, consistency, retry, page size, and load-balancing options. Prometheus, InfluxDB, and OpenTSDB now expose time-series posture panels plus native operation previews for query profiling, metrics/stats, cardinality analysis, retention updates, UID repair, access checks, line-protocol/API export, and guarded destructive metadata plans where the engine supports them; their connection UI and contract now preserve typed endpoint, path prefix, auth mode, tenant, Influx org/bucket/database/retention, default metric/range, TLS, and query-limit options without storing secret values. TimescaleDB now exposes PostgreSQL-wire plus native hypertable, chunk, compression, retention, continuous aggregate, and job branches, compact time-series posture panels, guarded compression/retention/refresh/import/export/backup previews, and PostgreSQL-wire row edits. Graph object views now expose guarded native previews for graph profile/metrics/index/security/export flows; their connection UI and contract preserve endpoint, database/graph, traversal-source, query-language, auth, AWS/SigV4, TLS, timeout, and fetch-size options without storing secret values. Warehouse object views now expose cost, compute, storage, and access posture panels plus Snowflake query-history/credit/warehouse-load/stream/share views, BigQuery job-timeline/reservation/slot/storage/IAM views, guarded Snowflake clone/suspend/resume, BigQuery copy, ClickHouse query-log/MergeTree/cluster posture, optimize, TTL materialization, freeze snapshot, explain/cost/metrics/access/import-export/drop previews; their connection UI and contract preserve endpoint, path prefix, account/project, database/dataset/schema, warehouse/role, local file, auth, TLS, timeout, row, cost, extension, and compute options without storing secret values. Cosmos DB object views now include partition, RU, indexing, and global distribution panels plus guarded previews for throughput, consistency, failover, query metrics, indexing, access, export, and drops. LiteDB object views now expose local-file posture plus guarded health, checkpoint, compact, backup, export, and drop previews. Memcached object views now expose cache/slab/item posture plus guarded stats, dump, reset, flush, and metadata workflows. The remaining work is not mainly "can the app list this engine"; it is making each engine deep enough to feel native and safe under real use.

PostgreSQL now adds compact storage, index-health, security, and activity posture panels, guarded vacuum/analyze/reindex previews in browser and Rust paths, and EXPLAIN results that emit shared `plan` payloads with table/raw fallbacks.

SQLite now adds compact local-file posture panels, PRAGMA/integrity cards, schema/index/trigger summaries, and guarded check/analyze/optimize/vacuum/reindex previews in browser and Rust paths.

Search and wide-column engines now have their Wave 3 contract-complete native UX slices plus Wave 8 edit hardening: Elasticsearch/OpenSearch expose parameterized HTTP-shaped search/admin/import/export plans with encoded paths, profile payloads, and explicit-id live document edits pinned by desktop manifest tests; DynamoDB exposes read API execution, consumed-capacity payloads, live guarded item edits pinned by desktop manifest tests, and AWS-shaped capacity/TTL/stream/backup/import/export plans; Cassandra exposes guarded CQL request-builder, tracing, partition-key warnings, cqlsh COPY, nodetool snapshot, and SSTable restore plans while staying contract-only for live binary execution.

Local, document, cache, and analytics engines now have their Wave 4 contract-complete native UX slices plus Wave 9 query/test hardening: Cosmos DB exposes RU, indexing, consistency, failover, metrics, access, import/export, and drop plans; LiteDB exposes local-file health, checkpoint, compact, index rebuild, backup, import/export, and drop plans; Memcached exposes stats, LRU dump, reset, flush, get/gets/set/touch/incr/decr/delete plans; DuckDB, ClickHouse, Snowflake, and BigQuery expose pinned SQL SELECT builders plus native profile/cost/metrics/access/import/export and engine-specific maintenance plans while keeping live mutation/admin/import execution as the explicit residual risk.

Time-series and graph engines now have their Wave 5 contract-complete native UX slices plus Wave 10 query/test hardening: Prometheus exposes a pinned PromQL range-builder descriptor, profile, metrics, target/rule/TSDB posture, and cardinality plans; InfluxDB exposes a pinned Flux/InfluxQL builder descriptor, profile, metrics, access, retention, import/export, and delete plans; OpenTSDB exposes a pinned metric-query builder descriptor, stats, UID repair, API export, and metadata delete plans; Neo4j, ArangoDB, JanusGraph, and Neptune expose pinned graph-query descriptors plus graph explain/profile, metrics, access/IAM, index, constraint/drop, graph import/export, and loader-style previews while keeping live driver/cloud execution as the explicit residual risk.

Search, wide-column, local/document/cache/analytics, time-series, and graph engines now have their Wave 11 deterministic IntelliSense slice: browser providers supply contract-backed query keys, SQL/CQL/DSL keywords, object names, fields, metrics, labels/tags, graph labels/relationships/properties, Memcached commands, known-key helpers, and bounded-query snippets without claiming live metadata-aware completion for every credentialed service.

Search, wide-column, local/document/cache/analytics, time-series, and graph engines now also have their Wave 12 object-tree parity slice: shared TypeScript tree manifests, Rust tree manifests, and browser-preview explorer routing all explicitly own the native roots and key sections for these engines. This is still contract parity; live capability hiding for cloud permissions, plugin availability, graph services, and version-specific metadata remains residual risk.

Search, wide-column, local/document/cache/analytics, time-series, and graph engines now have their Wave 13 connection-flow parity slice: typed shared option bags, right-drawer controls, browser request validation, Rust profile interpolation, timeout selection, and redaction all line up for the secondary families. This is still contract parity; live cloud auth, binary protocol/SASL execution, driver behavior, and credentialed service validation remain residual risk.

Search, wide-column, local/document/cache/analytics, time-series, and graph engines now have their Wave 14 guarded-operation parity slice: completeness criteria, object-view action helpers, browser operation manifests, browser operation planners, Rust operation manifests, Rust operation planners, disabled reasons, confirmation requirements, and plan-only execution wording all line up for the secondary families. This remains preview-first contract parity; live admin/write execution still requires adapter-backed permission, cost, environment, rollback, and fixture/cloud validation.

Search, wide-column, local/document/cache/analytics, time-series, and graph engines now have their Wave 15 diagnostics/performance parity slice: completeness criteria, diagnostics tree roots, object-view posture panels, browser diagnostics/profile/cost payloads, browser operation planners, Rust operation manifests, Rust metrics/profile planners, and live-sampling residual-risk wording all line up for the secondary families. This remains contract parity; live sampling, cloud metrics, profile dashboards, and fixture-backed telemetry still require adapter-specific validation.

Search, wide-column, local/document/cache/analytics, time-series, and graph engines now have their Wave 16 import/export parity slice: completeness criteria, object-view actions, browser operation manifests, browser operation planners, Rust operation manifests, Rust operation planners, backup/restore or bounded export plans, and plan-only file/cloud execution wording all line up for the secondary families. This remains contract parity; adapter-owned file pickers, cloud storage workflows, restore execution, and live fixture validation remain residual risk.

Search, wide-column, local/document/cache/analytics, time-series, and graph engines now have their Wave 17 object-view parity slice: completeness criteria, descriptor-backed workflows, focused descriptor tests, workspace routing, posture panels, and guarded action strips all line up for the secondary families. This remains contract parity; rendered live payload depth, plugin/capability-specific views, cloud permission payloads, and fixture-backed UI validation remain residual risk.

The current strongest live/native areas are MongoDB, Redis/Valkey, SQL Server, PostgreSQL, SQLite, and the shared result workbench. The full all-engine matrix is contract-complete; the weakest remaining areas are live depth, optional fixture/cloud validation, and native management flows for cloud, graph, warehouse, and some secondary engines.

Strictly, no datastore is fully native-complete yet. MongoDB and Redis/Valkey are the reference patterns, core SQL is usable and deepening, and secondary/cloud engines still need more live adapter behavior before they should lose beta/foundation framing.

The quality gate now enforces several production-native guardrails:

- every completeness profile must cover all native-completion criteria and explicitly name remaining work for non-strong areas
- every declared engine must be listed in the contract-complete acceptance set, expose a completion claim, and keep per-criterion contract evidence
- secondary connection-flow contracts must keep shared types, right-drawer fields, browser validation, Rust models, interpolation, timeout routing, and redaction in sync
- secondary object-view contracts must keep descriptor-backed workflows, focused descriptor tests, workspace routing, posture panels, guarded action strips, and live-depth caveats in sync
- secondary guarded-operation contracts must keep object-view actions, browser manifests/planners, Rust manifests/planners, disabled reasons, confirmation requirements, and plan-only warnings in sync
- secondary diagnostics/performance contracts must keep diagnostics roots, object-view posture panels, browser diagnostics/profile payloads, browser and Rust metrics/profile planners, and live-sampling caveats in sync
- secondary import/export contracts must keep object-view actions, browser manifests/planners, Rust manifests/planners, backup/restore or bounded export request shapes, and plan-only file/cloud execution caveats in sync
- production query starters and structural tree manifests must not invent fixture/default objects such as `products`, `catalog`, or synthetic table names
- object-view menu labels must be workflow-specific instead of generic `Open View`
- tree manifests must continue avoiding fake/sample leaves and hidden preview-only clutter

## 2026-06-03 Native UX Audit Notes

The current application has a purpose-built object-view workspace for every declared datastore family, so the remaining work is depth and honesty rather than first-time wiring. The following findings should guide the next implementation slices:

- **No datastore is native-complete yet.** MongoDB is closest; Redis/Valkey and core SQL are usable, with live guarded core edits now protected by reference-engine and SQL-family tests, but they still need management depth, native diagnostics polish, module depth, richer before/after previews, and import/export execution.
- **Production flows must stay live-metadata-only.** Browser-preview fixtures and test-suite templates still contain deterministic names such as `catalog`, `products`, `orders`, and `datapad_test_*`. Those are acceptable for tests/demo preview only; real desktop query creation, trees, and object views must not default to them.
- **Browser-preview fallback defaults are being retired before promotion.** Runtime helpers for MongoDB, Cosmos DB, DynamoDB, LiteDB, and Cassandra now fail closed for incomplete scoped targets instead of choosing fixture objects such as `catalog`, `products`, `Orders`, or `orders_by_customer`. Remaining preview fixtures should stay explicitly deterministic and never be used as fallback production targets.
- **MongoDB view result wording is now production-oriented.** View nodes expose `view-results` / `Results Preview` for bounded querying of real views; legacy `sample-results` is treated as a compatibility alias only.
- **Guarded plans are not the same as native management.** Many secondary engines now have good-looking operation chips and object-view cards, but the Rust paths still return preview-only or guarded request plans for writes/admin work. A datastore should not be considered native until safe live operations exist where users naturally expect them.
- **Capability hiding remains a release blocker.** Optional modules and managed-service features, such as Redis Stack, Atlas Search, OpenSearch security plugins, SQL Agent, Query Store, Oracle enterprise views, cloud IAM, and warehouse task/share surfaces, should only show when live metadata or a declared capability proves they are available.

## Readiness Matrix

| Datastore | Current readiness | What already exists | Remaining to be feature complete |
| --- | --- | --- | --- |
| MongoDB | Near-native | Native/URI connections, database discovery, Mongo tree, find builder, raw commands, scripting, aggregation surface, document renderer, field inspector, efficiency mode, explain UI, metrics, profiler/currentOp/replica/shard/index-usage diagnostic probes, object views, native guarded insert-document and field set/unset/rename/type-change edits, and insert/upload views. | Add richer before/after previews, delete/replace coverage, polished index/user/role/validator management, collection import/export execution, rendered profiler/currentOp/replica/shard dashboards, richer aggregation-stage IntelliSense. |
| Redis | Near-native | Native key browser, Redis console, scan filters, type summaries, key detail result view, metrics dashboard, Redis tree, diagnostics/object views, live guarded core key/member edits, TTL/rename/delete/import/export plans, capability-hiding tests, and Redis Stack/module folders hidden until live metadata proves availability. | Finish module type editors, command-argument completions, deeper Pub/Sub/streams/ACL/Lua/functions/cluster/sentinel panels, optional Redis Stack fixture coverage, and guarded live import/export execution. |
| Valkey | Usable | Shares Redis browser, console, key/result model, diagnostics, live guarded core edits, Valkey-specific manifest capability text, reference test coverage, and optional module hiding without Redis Stack/vector-only claims. | Verify Valkey fixture/live behavior, deepen Valkey-specific copy in tree menus, and finish module-aware edit parity only for confirmed Valkey-compatible modules. |
| PostgreSQL | Usable+ | Live SQL path, schema/table tree, raw SQL default, SELECT builder for scoped objects, object descriptors, desktop live-scope tests for primary-key row edits, compact storage/index/security/activity posture panels, metrics/diagnostics contracts, IntelliSense, rendered EXPLAIN plan payloads with table/raw fallbacks, and dialect-aware profile/index/grants/export/backup/vacuum/analyze/reindex previews. | Deepen live roles/grants and pg_stat payloads, EXPLAIN ANALYZE details, extension management views, before/after row-edit previews, and guarded live import/export and backup execution flows. |
| CockroachDB | Usable+ | PostgreSQL-wire foundation, Cockroach tree, live primary-key row edits, object descriptors for jobs/ranges/regions/contention, compact table/cluster/locality/job/contention/security posture panels, SQL query flow, read-safe Cockroach query guardrails for `SHOW JOBS`, `SHOW RANGES`, and `SHOW CLUSTER SETTING`, distributed explain/profile/import/export/backup preview hooks, guarded jobs/ranges/regions/sessions/contention/roles-grants/zone-config previews. | Deepen live `crdb_internal` jobs/ranges/regions/localities/contention payloads, rendered distributed explain/profile UI, backup/import/restore execution flows, role/default privilege workflows. |
| SQL Server / Azure SQL | Usable+ | TDS query path, SSMS-inspired tree, raw T-SQL default, object descriptors for databases/tables/procedures/functions/Query Store/Agent/security/storage, compact storage/index/workload/security/Agent posture panels, result grid, live primary-key row edits, rendered SHOWPLAN_TEXT payloads with table/raw fallbacks, Showplan/statistics/index/grants/export/backup previews, guarded Update Statistics, index maintenance, and Query Store workload previews. | Complete XML Showplan details, Extended Events, deeper Query Store/Agent/security/storage payloads, Azure auth disabled reasons, and guarded live import/export/compare execution flows. |
| MySQL | Usable | Native connection/query foundation, Workbench-style tree manifest, SQL editor, live primary-key row edits, shared relational result/edit contracts, storage/index/security/diagnostics posture panels, server-level users/roles/grants/sessions/status/slow-query branches, rendered EXPLAIN payloads, and guarded index/grants/export/check/analyze/optimize/repair/event previews. | Deepen live performance_schema payloads, EXPLAIN ANALYZE details, full user@host grant management, and import/export execution flows. |
| MariaDB | Usable | MySQL-compatible adapter base, Workbench-style tree/object descriptors, SQL editor, live primary-key row edits, MariaDB profile preview handling, shared relational contracts, storage/security/status panels, and guarded maintenance/event/import/export previews. | Add deeper MariaDB role semantics, optimizer trace/status dashboards, routine/event management, and import/export execution flows. |
| SQLite | Usable+ | Local file open/create, SQLite tree, raw SQL default, object descriptors, table results, local database fixture support, live primary-key row edits, rendered EXPLAIN QUERY PLAN/bytecode payloads, compact file/PRAGMA/schema posture panels, and guarded index/export/backup/check/analyze/optimize/vacuum/reindex previews. | Deepen live PRAGMA payloads, better trigger/index editing workflows, and local backup/export execution. |
| Oracle | Foundation/beta | Oracle connection option model, SQL/PLSQL/explain descriptors, SQL Developer-style tree, object views for schema/table/package/security/storage/performance, DBMS_XPLAN-shaped plan/profile payloads, guarded SQLcl/Data Pump import/export and RMAN backup/restore previews, preview metadata without required Oracle CI dependency. | Add live driver/runtime path, live DBMS_XPLAN execution, sessions/storage/security depth, dictionary permission fallbacks, compile/grant/DDL previews, optional live tests. |
| TimescaleDB | Usable | PostgreSQL-wire base, SQL SELECT builder exposure, browser preview-only SQL row-edit planning, desktop live-scope tests for primary-key row edits, native hypertable/chunk/compression/retention/continuous-aggregate/job tree, compact time-series posture panels, rendered PostgreSQL EXPLAIN payloads, and guarded compression-policy, retention-policy, aggregate-refresh, import/export, backup/restore, profile/index/grants previews. | Add richer live Timescale metadata execution, rendered time-bucket dashboards, compression/chunk sizing charts, continuous aggregate freshness history, before/after row-edit previews, and guarded live policy execution. |
| Elasticsearch | Foundation+ | HTTP adapter foundation, typed HTTP/Elastic Cloud/API key/default-index/TLS/timeout connection option contract, search tree, Query DSL builder, bounded live search execution, search hits renderer, desktop live-scope tests for explicit-id document edits, object descriptors for cluster/index/mapping/security/diagnostics, native cluster/field/shard/segment/lifecycle/ingestion/security panels, profile renderer, and guarded HTTP-shaped profile/explain/force-merge/cache/reindex/open-close/mapping/settings/alias/data-stream/template/pipeline/lifecycle/task/security/bulk/snapshot/restore previews. | Add live HTTPS/cloud auth execution flows, live admin execution flows, before/after document previews, aggregation builder polish, richer rendered explain/profile UI, slow-log/allocation dashboards, and bulk import/export execution. |
| OpenSearch | Foundation+ | Elasticsearch-like foundation with separate adapter identity, typed managed endpoint/AWS SigV4/default-index/TLS/timeout connection option contract, search descriptors, native search panels, profile renderer, desktop live-scope tests for explicit-id document edits, OpenSearch security and ISM-aware request routing, and guarded admin/import/export/snapshot plans. | Add OpenSearch-specific Performance Analyzer support, live SigV4/IAM, plugin capability detection, before/after document previews, rendered profile/admin workflows, and live guarded execution where safe. |
| DynamoDB | Foundation+ | Key-condition builder, typed local/AWS connection option UI and contract, read API execution, table tree, desktop live-scope tests for guarded item put/update/delete, capacity/cost/profile result shapes, object descriptors, native key-design/capacity/TTL/streams/backup/index panels, guarded object-view previews for CloudWatch metrics, capacity updates, TTL, streams, GSI create/delete, IAM-style access inspection, table backup/restore, table export/import, and table deletion. | Wire the typed options into full AWS SDK/SigV4 runtime coverage, before/after item previews, conditional-expression helpers, backup/restore execution, import/export file workflows, and deeper CloudWatch/account diagnostics. |
| Cassandra | Foundation+ | CQL builder scaffolding, typed contact-point/TLS/policy connection option UI and contract, keyspace/table tree, object descriptors, partition-model/storage/cluster-health panels, request-builder execution with partition-key/tracing warnings, browser-preview payloads, guarded object-view previews for tracing, SAI/index create/drop, permission inspection, diagnostics metrics, cqlsh COPY import/export, nodetool snapshot/SSTable restore, and object drops. | Add native binary protocol depth, live metadata, rendered tracing timelines, repair dashboards, SAI/index guidance, partition-key-safe live edits, and optional nodetool/JMX-backed diagnostics. |
| Cosmos DB | Foundation+ | Typed emulator/account-endpoint/Entra/managed-identity/resource-token connection options, account/container tree, document result contracts, partition/RU/indexing/global distribution panels, and guarded throughput/consistency/failover/profile/index/access/export/drop previews. | Add signed Azure auth, SQL API builder, live throughput/change-feed diagnostics, guarded item/import/delete execution workflows, and Azure portal parity for backups/private networking. |
| LiteDB | Foundation+ | Local database model, create/open direction, collection/index/file-storage tree, object-view posture panels, and guarded local health/checkpoint/compact/index/export/backup/drop previews. | Complete live sidecar bridge, collection/index/file-storage management execution, encrypted local file handling, import/export execution, document editing. |
| Memcached | Foundation+ | Typed server-list/protocol/SASL/timeout connection options, stats/slabs/items/settings tree, diagnostics descriptors, cache/slab/item posture panels, guarded stats/settings/reset/flush/metadata dump previews, and known-key get/gets/set/touch/incr/decr/delete previews. | Add live protocol/SASL execution, binary protocol coverage where useful, CAS-safe value editing, multi-node guardrails, and richer dashboard sampling. |
| DuckDB | Usable | Local OLAP tree, typed local-file/memory/extension/thread/memory-limit connection options, SQL SELECT builder coverage with no edit scopes, SQL/result foundation, object descriptors for schemas/tables/extensions/files/pragmas, local-file/file-analytics/extension/maintenance posture panels, guarded analyze/checkpoint/extension load-install/import-export/backup previews. | Add live CSV/Parquet import execution, extension manager execution, deeper PRAGMA views, rendered explain/profile UI, backup/export execution, and richer local analytics templates. |
| ClickHouse | Foundation+ | Warehouse tree, typed HTTP/native endpoint/database/auth/TLS/row-limit connection options, SQL SELECT builder coverage with no edit scopes, SQL/HTTP-oriented contract, object descriptors, query/log/metrics surfaces, cost/compute/storage/access posture panels, ClickHouse query-log/MergeTree/cluster/maintenance posture panels, and guarded EXPLAIN/profile/metrics/access/export/optimize/TTL/freeze/drop previews. | Add robust native protocol behavior, rendered system.query_log dashboards, EXPLAIN pipeline UI, live cluster metrics, live import/export, and guarded execution for maintenance workflows. |
| Snowflake | Foundation+ | Cloud warehouse contract, typed account/endpoint/database/schema/warehouse/role/auth/TLS/row/cost connection model, SQL SELECT builder coverage with no edit scopes, warehouse/stage/job/security tree, object descriptors, query-history, credit, warehouse-load, stream/share posture panels, and guarded explain/cost/metrics/access/export/clone/suspend/resume/drop previews. | Add live OAuth/programmatic auth modes, role/warehouse selectors, rendered query profile graph, live utilization/history queries, import/export execution, and task/stream/share management execution. |
| BigQuery | Foundation+ | Cloud warehouse contract, typed endpoint/project/dataset/location/auth/row/cost connection options, SQL SELECT builder coverage with no edit scopes, project/dataset/table/job tree, dry-run/cost model, object descriptors, job-timeline, reservation, slot-usage, table-storage, IAM posture panels, and guarded dry-run/IAM/metrics/export/copy previews. | Add ADC/service account auth execution, INFORMATION_SCHEMA live metrics, IAM execution, scheduled query management, dry-run-first workflow, export/import jobs. |
| Prometheus | Foundation+ | PromQL language, typed endpoint/auth/path-prefix/tenant/range connection options, metric/label/target/rule tree, pinned PromQL range-builder descriptor with no edit scopes, chart/series result contracts, object descriptors, posture panels, native profile, metrics/stats, and cardinality operation previews. | Add native PromQL builder depth, deeper targets/rules/alerts/TSDB live views, range chart polish, optional live profile/cardinality execution. |
| InfluxDB | Foundation+ | Typed v1/v2/v3 endpoint/auth/org/bucket/database/retention/query-language connection options, bucket/measurement/tag/field tree, pinned Flux/InfluxQL builder descriptor with no edit scopes, time-series result contracts, object workspace foundation, posture panels, native profile/metrics/access/export/delete and retention update previews. | Add deeper token/task/retention management views, cardinality diagnostics, line protocol import execution, guarded live bucket/task workflows. |
| OpenTSDB | Foundation+ | Typed endpoint/auth/path-prefix/default-metric connection options, metric/tag/aggregator/downsample tree, pinned metric-query builder descriptor with no edit scopes, series result contract, object descriptors, posture panels, native stats/export/delete and UID repair metadata operation previews. | Add metric/tag query builder depth, UID/tree management execution, stats dashboards, HBase/backend health guidance, live API export/import flows. |
| Neo4j | Foundation+ | Graph family contracts, typed endpoint/database/auth/TLS/query options, pinned Cypher pattern-builder descriptor with no edit scopes, tree/object descriptors, graph renderer shape, guarded EXPLAIN/PROFILE, JMX metrics, index/security/constraint/drop, and graph export previews. | Add live Bolt/Cypher depth, richer graph visualization, rendered EXPLAIN/PROFILE operator graphs, full schema/index/constraints/security execution, safe graph operations. |
| ArangoDB | Foundation+ | Graph/document tree foundation, typed endpoint/database/graph/auth/TLS/query options, pinned AQL graph-builder descriptor with no edit scopes, graph workspace descriptors, guarded AQL explain/profile, metrics, security, index, drop, and export previews. | Add deeper live HTTP/AQL execution, collections/graphs/indexes, graph views, rendered explain/profile, Foxx/security execution later. |
| JanusGraph | Foundation+ | Gremlin/graph contract foundation, typed endpoint/traversal-source/auth/TLS/query options, pinned Gremlin traversal-builder descriptor with no edit scopes, descriptor routing, traversal explain/profile, management metrics, index lifecycle, backend-health, and graph export previews. | Add live Gremlin depth, schema/index/property views, backend health, reindex operation execution previews, and optional backend/index-service fixtures. |
| Neptune | Foundation+ | Graph cloud contract foundation, typed endpoint/path/database/language/AWS SigV4/IAM/TLS options, pinned Gremlin/openCypher builder descriptor with no edit scopes, loader-job concepts, and guarded Gremlin explain/profile, CloudWatch, IAM access, and import/export previews. | Add Gremlin/openCypher/SPARQL mode switching, live IAM/SigV4, loader jobs, rendered explain/profile, CloudWatch metrics dashboards. |

## Cross-Cutting Work Still Required

These items affect many engines and should be finished before declaring the platform production-ready:

1. Live depth: replace remaining browser-preview or plan-only paths with real adapter calls where drivers are realistic.
2. Capability hiding: optional tree nodes must stay hidden until live metadata or capability checks prove they exist.
3. Object views: every visible tree item should open a purpose-built workspace, not a raw payload dump. Workflow actions should be adapter-owned, hidden when unavailable, and linked to a real query or view section.
4. Safe edits: enable live edits only for complete identities and keep read-only/environment guards centralized.
5. Diagnostics: each engine needs native explain/profile/metrics/history views, not just generic counters.
6. Import/export: move from result-level export to engine-native import/export and backup/restore workflows.
7. Connection forms: finish datastore-specific connection modes without bloating the generic drawer.
8. IntelliSense: deepen metadata-aware suggestions for routines, aliases, indexes, document paths, commands, and cloud objects.
9. Tests: add deterministic UI/browser-preview tests for every visible tree/object-view action and optional fixture/live tests for promoted engines.

## Completed Native-Completion Slices

- **2026-06-03: Wave 0 quality gate stabilization.** The current in-progress scoped-target/runtime slice now passes `npm run check:all` without raising module-size budgets. Oversized React state/runtime and Rust scoped-tab modules were split around command wrappers, save helpers, connection-health reduction, Redis scoped-tab helpers, and scoped-tab tests. A quality drift test now keeps datastore engine declarations synchronized across the shared engine list, roadmap backlog, tree manifests, Rust registry dispatch, and object-view descriptor ownership.
- **2026-06-03: No fake scoped targets.** Browser/runtime scoped target helpers now keep incomplete MongoDB, Cosmos DB, DynamoDB, LiteDB, and Cassandra targets empty or in inspect-only states instead of defaulting to fixture object names. Desktop scoped Mongo query creation has a matching regression so right-click/query flows cannot silently target `products` when the selected object is only a connection or malformed node.
- **2026-06-03: Preview capability hiding.** Browser-preview Redis now matches the shared manifest by showing core databases, Pub/Sub, Lua, ACL, and diagnostics while keeping optional Redis Functions/module surfaces out unless live metadata supplies them. SQL Server preview database folders now keep optional Agent/Extended Events surfaces out of the core database tree, and blank SQL Server profiles no longer invent a `datapadplusplus` database or `dbo.object` target.
- **2026-06-03: Wave 1 Redis/Valkey reference contracts.** Redis and Valkey runtime manifests now advertise the same key browser, TTL, guarded admin, ACL/permission, streaming, import/export, metrics, and structure capabilities that the shared roadmap exposes. Redis keeps vector-search capability; Valkey intentionally omits Redis Stack/vector-only claims. Native Redis database type folders now show core key types by default and add JSON, time-series, Bloom, search, or vector branches only after `MODULE LIST` detects those modules. Redis/Valkey operation manifests and generated previews now cover key export/import, rename, duplicate, move, expire/persist, and stream ack/delete plans.
- **2026-06-03: Wave 1 MongoDB diagnostics probes.** MongoDB adapter diagnostics now add optional profiler status, recent `system.profile` entries, `currentOp`, replica-set status, sharding state, and collection-scoped `$indexStats` probes. These remain read-only and permission-aware: unavailable probes add warnings rather than blocking the metrics already collected from `serverStatus` and `dbStats`.
- **2026-06-03: Wave 2 PostgreSQL rendered plan payloads.** PostgreSQL EXPLAIN execution now emits a shared `plan` renderer payload with normalized plan lines, original table rows, JSON metadata, metrics, and raw text fallback so the existing result workbench can render it consistently with MySQL, SQLite, and MongoDB plans.
- **2026-06-03: Wave 2 core SQL contract checkpoint.** SQL Server SHOWPLAN_TEXT now emits shared `plan` payloads with table/raw fallbacks, TimescaleDB now exposes PostgreSQL-wire live row edits plus import/export and backup/restore operation manifests, TimescaleDB operation plans now use chunk-aware export and pg_dump/pg_restore previews, and Oracle generated operation requests now include SQLcl/Data Pump import/export plus RMAN backup/restore previews while staying contract-only for live execution.
- **2026-06-03: Wave 3 search and wide-column contract checkpoint.** Search operation manifests now cover mapping/settings, aliases, lifecycle explain, data-stream rollover, templates, pipeline simulation, bulk import/export, snapshot, and restore plans with parameterized and encoded HTTP request shapes. DynamoDB manifests now cover capacity, TTL, streams, backup create/restore, generic backup/restore, and import/export plans; Cassandra now advertises import/export plus backup/restore and generates cqlsh COPY, nodetool snapshot, and SSTable restore plans while keeping live CQL execution as the explicit residual risk.
- **2026-06-03: Wave 4 local/document/cache/analytics contract checkpoint.** Cosmos DB and LiteDB now expose document-native operation manifests for throughput, consistency, regional failover, checkpoint, compact, index rebuild, backup, import/export, metrics, access, and destructive plans. Memcached now exposes stats reset, flush, CAS reads, set/touch/increment/decrement/delete, metrics, and LRU dump previews. DuckDB, ClickHouse, Snowflake, and BigQuery are promoted to Wave 4 contract-complete native UX in the shared completeness matrix with explicit live/cloud/local validation residual risks.
- **2026-06-03: Wave 5 time-series/graph contract checkpoint.** Prometheus, InfluxDB, and OpenTSDB now expose time-series-native operation manifests for cardinality, retention, UID repair, metrics, access, import/export, and guarded destructive plans. Neo4j, ArangoDB, JanusGraph, and Neptune now expose graph explain/profile, metrics, access/IAM, index, graph export/import, and destructive previews with browser and Rust request-shape parity. All seven Wave 5 engines are promoted to contract-complete native UX in the shared completeness matrix with explicit fixture/live validation residual risks.
- **2026-06-03: Final all-engine contract acceptance.** The shared completeness matrix now separates native readiness from the contract-complete release claim. All 29 declared engines are listed in `CONTRACT_COMPLETE_DATASTORE_ENGINES`, every criterion carries `contractStatus`, `evidence`, and `contractNote` fields, browser datastore experiences expose the claim, and the quality gate compares the acceptance set to the declared engine registry so future datastore additions cannot drift silently.
- **2026-06-03: Wave 6 reference-engine hardening.** MongoDB safe editing is now marked strong in the shared matrix because the desktop adapter supports guarded live insert-document and field set/unset/rename/type-change edits when identity and environment checks pass. Redis and Valkey reference tests now pin live native key/member edit scopes on the desktop adapter while browser-preview experiences remain explicitly plan-only. MongoDB import/export wording now reflects collection-level native plans without implying live file execution.
- **2026-06-03: Wave 7 core SQL hardening.** PostgreSQL, CockroachDB, SQL Server/Azure SQL, MySQL, MariaDB, SQLite, and TimescaleDB now mark safe editing and tests as strong in the shared matrix because desktop manifests pin live primary-key row-edit scopes while browser-preview manifests remain plan-only. TimescaleDB also now exposes the SQL SELECT builder and SQL-shaped row-edit preview guardrails despite its time-series family classification. Oracle keeps its separate contract-only row-edit residual risk until a live driver path exists.
- **2026-06-03: Wave 8 search and wide-column hardening.** Elasticsearch and OpenSearch now mark explicit-id document safe editing and tests as strong in the shared matrix, with desktop manifests pinning live index-document/update-document/delete-document scopes while browser-preview manifests remain plan-only. DynamoDB now has a separate profile from Cassandra so complete-key item put/update/delete editing and tests are strong for DynamoDB, while Cassandra keeps its contract-only live CQL row-edit residual risk.
- **2026-06-03: Wave 9 local/document/cache/analytics hardening.** Cosmos DB, LiteDB, and Memcached now mark deterministic Wave 4 test coverage as strong in the shared matrix while keeping edits and import/export execution preview-first. DuckDB, ClickHouse, Snowflake, and BigQuery now mark query surface and tests as strong because browser and desktop manifests pin SQL SELECT builders, and both manifests keep editable scopes empty until adapter-owned mutation safety exists.
- **2026-06-03: Wave 10 time-series and graph hardening.** Prometheus, InfluxDB, OpenTSDB, Neo4j, ArangoDB, JanusGraph, and Neptune now mark query surface and tests as strong because browser and desktop manifests pin time-series or graph query-builder descriptors, and both manifests keep editable scopes empty until adapter-owned mutation safety exists.
- **2026-06-03: Wave 11 deterministic IntelliSense hardening.** Search, DynamoDB/Cassandra, Cosmos DB, LiteDB, Memcached, DuckDB, ClickHouse, Snowflake, BigQuery, Prometheus, InfluxDB, OpenTSDB, Neo4j, ArangoDB, JanusGraph, and Neptune now mark IntelliSense as strong for the contract slice because browser providers cover deterministic keywords, objects, fields, commands, metrics, dimensions, graph schema concepts, and bounded snippets while leaving live metadata-aware suggestions as residual risk.
- **2026-06-03: Wave 12 secondary object-tree parity.** Search, DynamoDB/Cassandra, Cosmos DB, LiteDB, Memcached, DuckDB, ClickHouse, Snowflake, BigQuery, Prometheus, InfluxDB, OpenTSDB, Neo4j, ArangoDB, JanusGraph, and Neptune now mark object trees as strong for the contract slice because shared TypeScript manifests, Rust manifests, and browser explorer routing pin native roots and key sections without claiming live capability detection for every service mode.
- **2026-06-03: Wave 13 secondary connection-flow parity.** Search, DynamoDB/Cassandra, Cosmos DB, LiteDB, Memcached, DuckDB, ClickHouse, Snowflake, BigQuery, Prometheus, InfluxDB, OpenTSDB, Neo4j, ArangoDB, JanusGraph, and Neptune now mark connection flow as strong for the contract slice because shared option bags, right-drawer fields, browser validation, Rust models, interpolation, timeout selection, and redaction stay synchronized. Memcached now has a dedicated typed server-list/SASL/timeout option contract while live protocol/SASL validation remains residual risk.
- **2026-06-03: Wave 14 secondary guarded-operation parity.** Search, DynamoDB/Cassandra, Cosmos DB, LiteDB, Memcached, DuckDB, ClickHouse, Snowflake, BigQuery, Prometheus, InfluxDB, OpenTSDB, Neo4j, ArangoDB, JanusGraph, and Neptune now mark guarded operations as strong for the contract slice because object-view actions, browser manifests/planners, Rust manifests/planners, disabled reasons, confirmation requirements, and plan-only warnings stay synchronized. Live admin/write execution remains the residual risk.
- **2026-06-03: Wave 15 secondary diagnostics/performance parity.** Search, DynamoDB/Cassandra, Cosmos DB, LiteDB, Memcached, DuckDB, ClickHouse, Snowflake, BigQuery, Prometheus, InfluxDB, OpenTSDB, Neo4j, ArangoDB, JanusGraph, and Neptune now mark diagnostics/performance as strong for the contract slice because diagnostics tree roots, object-view posture panels, browser diagnostics/profile payloads, browser planners, Rust metrics/profile planners, and live-sampling caveats stay synchronized. Live telemetry, profile dashboards, and cloud/fixture sampling remain the residual risk.
- **2026-06-03: Wave 16 secondary import/export parity.** Search, DynamoDB/Cassandra, Cosmos DB, LiteDB, Memcached, DuckDB, ClickHouse, Snowflake, BigQuery, Prometheus, InfluxDB, OpenTSDB, Neo4j, ArangoDB, JanusGraph, and Neptune now mark import/export as strong for the contract slice because object-view actions, browser manifests/planners, Rust manifests/planners, backup/restore or bounded export request shapes, and plan-only file/cloud execution caveats stay synchronized. Adapter-owned file/cloud execution and live restore/import validation remain the residual risk.
- **2026-06-03: Wave 17 secondary object-view parity.** Search, DynamoDB/Cassandra, Cosmos DB, LiteDB, Memcached, DuckDB, ClickHouse, Snowflake, BigQuery, Prometheus, InfluxDB, OpenTSDB, Neo4j, ArangoDB, JanusGraph, and Neptune now mark object views as strong for the contract slice because descriptor-backed workflows, focused descriptor tests, workspace routing, posture panels, and guarded action strips stay synchronized. Live payload depth, plugin/capability-specific screens, cloud permission details, and fixture-backed UI validation remain the residual risk.
- **2026-06-03: Blank profile fail-closed pass.** Browser-preview MySQL/MariaDB, CockroachDB, Oracle, InfluxDB, and graph helpers now avoid invented database/container/bucket/graph roots for blank profiles. Incomplete MySQL/PostgreSQL/Cockroach table scopes also resolve to no target instead of defaulting to demo tables such as `accounts`.
- **2026-06-03: MongoDB view result wording.** MongoDB view child nodes now use `view-results` and the visible label `Results Preview`; the old `sample-results` kind remains a compatibility alias for older saved/open state, not a new tree surface.
- **2026-06-03: Preview payload copy cleanup.** Cosmos diagnostics, warehouse external-stage metadata, and search pipeline simulation plans no longer display demo sample paths or sample document payloads when no real user input exists.

## Recommended Completion Order

1. Follow the active order in [Native Datastore Completion Tracker](native-completion-tracker.md), starting with MongoDB.
2. Finish native-complete reference datastores: MongoDB, Redis, Valkey.
3. Productionize core SQL live depth: SQLite, PostgreSQL, SQL Server/Azure SQL, MySQL, MariaDB, CockroachDB, TimescaleDB, Oracle.
4. Add optional live validation tracks for search and wide-column: DynamoDB, Elasticsearch, OpenSearch, Cassandra.
5. Add optional fixture/cloud validation for Wave 4 and Wave 5 engines while keeping default CI dependency-free.
6. Promote selected contract-only operation previews to guarded live execution once adapter-owned permission, cost, environment, rollback, and confirmation checks exist.

## Documentation Rule

When datastore features change, update these together:

- user-facing summary in `README.md`
- product feature details in `docs/features.md`
- implementation/reference status in this file
- active queue and per-engine progress in `docs/architecture/native-completion-tracker.md`
- adapter backlog or completeness source in `packages/shared-types/src/datastore-roadmap.ts` or `packages/shared-types/src/datastore-completeness.ts` when the actual capability changes
