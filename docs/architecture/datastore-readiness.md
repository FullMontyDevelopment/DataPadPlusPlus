# Datastore Readiness And Completion Plan

Last reviewed: 2026-06-07

This document is the current product inventory for DataPad++ datastore support. It is meant to answer one question clearly: what still has to be finished before each datastore feels like a native, production-ready IDE inside DataPad++?

The evaluation is based on the current shared adapter backlog, completeness matrix, tree manifests, object-view descriptors, browser-preview explorers, and Rust adapter registry:

- `packages/shared-types/src/datastore-roadmap.ts`
- `packages/shared-types/src/datastore-completeness.ts`
- `packages/shared-types/src/datastore-tree-manifests.ts`
- `docs/architecture/native-completion-tracker.md`
- `apps/desktop/src/app/components/workbench/*ObjectViewDescriptors.ts`
- `apps/desktop/src/app/components/workbench/*ObjectViewWorkspace.tsx`
- `apps/desktop/src-tauri/src/adapters/registry.rs`

## Feature Complete Definition

A datastore is feature complete when it has all of these:

- native connection options, connection strings where appropriate, variables, secret safety, and friendly connection errors
- a live metadata tree that feels like the datastore's own tool, hides unavailable sections, and supports scoped refresh
- native query surfaces such as SQL editor, document builder, key browser, console, script mode, or graph/time-series builder
- metadata-aware IntelliSense from cached explorer/structure data, not per-keystroke metadata calls
- purpose-built object views for the engine's important concepts, without default raw payload dumps
- safe live edits for natural row/document/key/item changes only when the target identity is complete
- guarded admin/destructive operation previews with environment checks, read-only checks, and clear disabled reasons
- diagnostics and performance views such as explain/profile, sessions, locks, metrics, slow queries, cost, or capacity
- import/export or backup/restore workflows that match the datastore
- deterministic unit/browser-preview tests plus optional fixture/live tests when external dependencies are available

## Native Completion Tracker

The execution order, active datastore, per-engine native gaps, and acceptance gate are tracked in [Native Datastore Completion Tracker](native-completion-tracker.md). When a datastore completion slice starts or finishes, update that tracker in the same change as the shared completeness matrix and user-facing docs.

## Current Product Shape

DataPad++ now has a broad adapter foundation across 29 engines. Every declared engine is explicitly accepted for the contract-complete native UX gate in `packages/shared-types/src/datastore-completeness.ts`, with per-criterion contract evidence, a completion claim, and residual-risk wording that separates deterministic contract coverage from live production validation. Most engines have declared manifests, tree manifests, object-view descriptors, browser-preview payloads, operation manifests, diagnostics contracts, and test coverage. Secondary datastore connection flows now have shared/browser/Rust parity for typed option bags, right-drawer fields, browser validation, Rust interpolation, timeout selection, and secret-safe redaction; Memcached now has its own server-list/SASL/timeout option contract instead of relying only on generic host fields. Secondary datastore object views now use adapter-owned workflow descriptors that hide unavailable actions and jump to the relevant section instead of showing passive or generic chips. SQL-family object views expose guarded explain/profile/index/grants/export/backup previews through the shared operation-planning path, with dialect-aware browser and Rust previews, and SQL connections now have a visual table-and-relationship explorer for focused schema graph work. Core SQL now has live primary-key row editing across PostgreSQL-family, SQL Server, MySQL/MariaDB, SQLite, TimescaleDB, and Oracle; Oracle uses configured SQLPlus live execution with primary-key or ROWID evidence for its scoped row-edit claim. MySQL is native-complete for the scoped claim with typed native connection/profile options for SSL/auth/session/socket/cache/timeouts, a dialect-aware SELECT builder, MySQL-specific IntelliSense and query-helper snippets, Workbench-style security and diagnostics branches, compact storage/index/user/session/performance_schema posture panels, detailed statement-digest/table-I/O/metadata-lock/optimizer-trace/status-counter object-view sections, live status/session/wait/statement-digest/table-I/O/metadata-lock/InnoDB/optimizer-trace diagnostics, guarded check/analyze/optimize/repair/event previews, guarded desktop CSV/JSON/NDJSON table import/export, bounded JSON/SQL logical backup packages, and restore-package validation; MariaDB is native-complete for the scoped claim with the MySQL-compatible live SQL/edit base plus typed MariaDB connection/profile metadata, MariaDB-aware Workbench-style trees, explicit `EXPLAIN FORMAT=JSON`/`ANALYZE FORMAT=JSON` previews, MariaDB-specific IntelliSense helpers, native MariaDB object-view descriptors and posture cards for role mappings, server variables, storage engines, and `ANALYZE FORMAT=JSON`, status/version/storage-engine diagnostics, Aria metrics, role-mapping security previews, preview-first routine/event/admin/user workflows, guarded desktop CSV/JSON/NDJSON table import/export, bounded JSON/SQL logical backup packages, and restore-package validation. Search-family object views now include cluster, field-capability, shard-health, Lucene segment, lifecycle, ingestion, and security posture panels, a profile result renderer, and guarded HTTP-shaped previews for explain/profile, force merge, cache clear, reindex, open/close, mapping/settings updates, alias work, data-stream rollover, template create/delete, pipeline update/simulate, lifecycle policy update, task cancel, security, bulk export, snapshot, and restore workflows. DynamoDB object views now include native key-design, capacity, TTL, stream, backup, hot-partition, and index-coverage panels plus guarded AWS-shaped previews for capacity, TTL, streams, backups, metrics, GSI changes, IAM-style access, import/export, and table deletion; its connection UI and contract now preserve typed local endpoint, AWS profile, static key, assume-role, web-identity, retry, timeout, and consumed-capacity options without storing secret values. Cassandra object views now include partition-model, storage/compaction, tombstone, diagnostics, and cluster-health panels plus guarded previews for tracing, index work, permission checks, diagnostics, imports/exports, and destructive table/object plans; its connection UI and contract now preserve contact points, secure bundles, TLS files, local datacenter, consistency, retry, page size, and load-balancing options. Prometheus, InfluxDB, and OpenTSDB now expose time-series posture panels plus native operation previews for query profiling, metrics/stats, cardinality analysis, retention updates, UID repair, access checks, line-protocol/API export, and guarded destructive metadata plans where the engine supports them; their connection UI and contract now preserve typed endpoint, path prefix, auth mode, tenant, Influx org/bucket/database/retention, default metric/range, TLS, and query-limit options without storing secret values. TimescaleDB now exposes PostgreSQL-wire plus native hypertable, chunk, compression, retention, continuous aggregate, and job branches, richer live catalog normalizers for chunk ranges, compression coverage, aggregate freshness, job statistics, and job reliability, compact time-series posture panels, guarded compression/retention/refresh/import/export/backup previews, and PostgreSQL-wire row edits with before/after `RETURNING *` evidence plans. Graph object views now expose guarded native previews for graph profile/metrics/index/security/export flows; their connection UI and contract preserve endpoint, database/graph, traversal-source, query-language, auth, AWS/SigV4, TLS, timeout, and fetch-size options without storing secret values. Warehouse object views now expose cost, compute, storage, and access posture panels plus Snowflake query-history/credit/warehouse-load/stream/share views, BigQuery job-timeline/reservation/slot/storage/IAM views, guarded Snowflake clone/suspend/resume, BigQuery copy, ClickHouse query-log/MergeTree/cluster posture, optimize, TTL materialization, freeze snapshot, explain/cost/metrics/access/import-export/drop previews; their connection UI and contract preserve endpoint, path prefix, account/project, database/dataset/schema, warehouse/role, local file, auth, TLS, timeout, row, cost, extension, and compute options without storing secret values. Cosmos DB object views now include partition, RU, indexing, and global distribution panels plus guarded previews for throughput, consistency, failover, query metrics, indexing, access, export, and drops. LiteDB object views now expose local-file posture plus guarded health, checkpoint, compact, backup, export, and drop previews. Memcached object views now expose cache/slab/item posture plus guarded stats, dump, reset, flush, and metadata workflows. The remaining work is not mainly "can the app list this engine"; it is making each engine deep enough to feel native and safe under real use.

PostgreSQL now adds typed native connection/profile options for TCP, Unix socket, Cloud SQL proxy, managed metadata, application name, search path, target session attributes, TLS certificate paths, and connect/statement/lock/idle timeouts with right-drawer fields, Rust interpolation, timeout routing, and encoded DSN parameters. It also adds compact storage, index-health, security, extension, and activity posture panels, extension folders with update hints, extension-owned object rows, role memberships, default privileges, normalized schema/table/routine/sequence grants, PostgreSQL-aware IntelliSense for `pg_catalog` helpers, routine call/definition snippets, profile/session/lock snippets, and safe identifier quoting, guarded parameterized routine execution plans from function/procedure object views, guarded backend cancel/terminate previews with PID and current-backend checks, desktop row-edit before/after evidence from `RETURNING *` mutations and bounded primary-key prefetches, guarded vacuum/analyze/reindex previews, guarded role grant/revoke and extension update/drop previews, EXPLAIN results that emit shared `plan` payloads with table/raw fallbacks, live diagnostics profiles for `pg_stat_activity`, `pg_locks`, `pg_stat_user_tables`, and optional `pg_stat_statements`, guarded `EXPLAIN (ANALYZE, BUFFERS, VERBOSE, FORMAT JSON)` profile request plans, plus guarded desktop CSV/JSON/NDJSON table export/import and bounded JSON/SQL logical backup workflows. Full pg_dump/restore parity remains a preview-first residual unless it enters the scoped release claim.

LiteDB's active native-completion slice now adds local-file read/write open preflight, password/encryption posture, explicit lock-boundary metadata, `dotnet-litedb-sidecar` execution-boundary contracts, configured sidecar read-dispatch evidence with deterministic fixture-token plus local sidecar-process bounded response normalization, timeout clamps, process open-failure mapping, redaction checks, and sidecar-shaped full-document CRUD plans to diagnostics, inspection payloads, query profiles, browser planners, Rust planners, and the default validator. An opt-in .NET LiteDB sidecar project and validator now create a temporary real LiteDB database and validate collection listing, bounded reads, index metadata, guarded insert/update/delete, before/after reads, read-only mutation blocking, `_id` mismatch blocking, missing-file error mapping, and redaction.

SQLite now adds compact local-file posture panels, PRAGMA/integrity cards, schema/index/trigger summaries, guarded check/analyze/optimize/vacuum/reindex previews in browser and Rust paths, and guarded desktop live file workflows for `VACUUM INTO` backup plus CSV/JSON/NDJSON table export/import.

Search engines are native-complete for scoped plain-HTTP workflows while wide-column engines keep their Wave 3 contract-complete native UX slices plus DynamoDB native hardening: Elasticsearch/OpenSearch expose parameterized HTTP-shaped search/admin/import/export plans with encoded paths, aggregation-aware Query DSL helpers, normalized profile-stage payloads, explicit-id live document edits with before/after `_doc` evidence pinned by desktop/Rust tests, slow-log/allocation diagnostics, optional search fixture evidence, and OpenSearch SQL/ISM/security/Performance Analyzer boundary evidence; DynamoDB exposes read API execution including guarded read-only PartiQL `ExecuteStatement`, consumed-capacity and pagination payloads, live guarded item edits with consistent before/after `GetItem` evidence pinned by desktop manifest tests, and AWS-shaped capacity/TTL/stream/backup/import/export plans; Cassandra exposes guarded CQL request-builder, tracing, partition-key warnings, cqlsh COPY, nodetool snapshot, and SSTable restore plans while staying contract-only for live binary execution.

Local, document, cache, and analytics engines now have their Wave 4 contract-complete native UX slices plus Wave 9 query/test hardening: Cosmos DB exposes RU, indexing, consistency, failover, metrics, access, import/export, and drop plans; LiteDB exposes local-file health, read/write open preflight, encryption and lock-boundary metadata, configured sidecar read-dispatch contracts with local sidecar-process evidence, optional .NET engine read/edit validation, sidecar-only full-document CRUD plans, checkpoint, compact, index rebuild, backup, import/export, and drop plans; Memcached exposes stats, LRU dump, reset, flush, get/gets/set/touch/incr/decr/delete plans; DuckDB, ClickHouse, Snowflake, and BigQuery expose pinned SQL SELECT builders plus native profile/cost/metrics/access/import/export and engine-specific maintenance plans, with DuckDB now adding guarded CSV file execution, database-file preflight/read-only guard evidence, explicit scoped file-workflow lock-boundary metadata, fail-closed JSON/Parquet extension-backed format gates with preloaded-extension-only boundaries, restore-package preflight, explicit restore execution-boundary metadata, structured extension install/load gates with explicit extension execution-boundary metadata, and structured analyze/checkpoint/object admin-scope gates with explicit admin execution-boundary metadata while unpromoted mutation/admin/extension execution remains an explicit optional extension.

Time-series and graph engines now have their Wave 5 contract-complete native UX slices plus Wave 10 query/test hardening: Prometheus exposes a pinned PromQL range-builder descriptor, profile, metrics, target/rule/TSDB posture, and cardinality plans; InfluxDB exposes a pinned Flux/InfluxQL builder descriptor, profile, metrics, access, retention, import/export, and delete plans; OpenTSDB exposes a pinned metric-query builder descriptor, stats, UID repair, API export, and metadata delete plans; Neo4j, ArangoDB, JanusGraph, and Neptune expose pinned graph-query descriptors plus graph explain/profile, metrics, access/IAM, index, constraint/drop, graph import/export, and loader-style previews while keeping live driver/cloud execution as the explicit residual risk.

Search, wide-column, local/document/cache/analytics, time-series, and graph engines now have their Wave 11 deterministic IntelliSense slice: browser providers supply contract-backed query keys, SQL/CQL/DSL keywords, object names, fields, metrics, labels/tags, graph labels/relationships/properties, Memcached commands, known-key helpers, and bounded-query snippets without claiming live metadata-aware completion for every credentialed service.

Search, wide-column, local/document/cache/analytics, time-series, and graph engines now also have their Wave 12 object-tree parity slice: shared TypeScript tree manifests, Rust tree manifests, and browser-preview explorer routing all explicitly own the native roots and key sections for these engines. This is still contract parity; live capability hiding for cloud permissions, plugin availability, graph services, and version-specific metadata remains residual risk.

Search, wide-column, local/document/cache/analytics, time-series, and graph engines now have their Wave 13 connection-flow parity slice: typed shared option bags, right-drawer controls, browser request validation, Rust profile interpolation, timeout selection, and redaction all line up for the secondary families. This is still contract parity; live cloud auth, binary protocol/SASL execution, driver behavior, and credentialed service validation remain residual risk.

Search, wide-column, local/document/cache/analytics, time-series, and graph engines now have their Wave 14 guarded-operation parity slice: completeness criteria, object-view action helpers, browser operation manifests, browser operation planners, Rust operation manifests, Rust operation planners, disabled reasons, confirmation requirements, and plan-only execution wording all line up for the secondary families. This remains preview-first contract parity; live admin/write execution still requires adapter-backed permission, cost, environment, rollback, and fixture/cloud validation.

Search, wide-column, local/document/cache/analytics, time-series, and graph engines now have their Wave 15 diagnostics/performance parity slice: completeness criteria, diagnostics tree roots, object-view posture panels, browser diagnostics/profile/cost payloads, browser operation planners, Rust operation manifests, Rust metrics/profile planners, and live-sampling residual-risk wording all line up for the secondary families. This remains contract parity; live sampling, cloud metrics, profile dashboards, and fixture-backed telemetry still require adapter-specific validation.

Search, wide-column, local/document/cache/analytics, time-series, and graph engines now have their Wave 16 import/export parity slice: completeness criteria, object-view actions, browser operation manifests, browser operation planners, Rust operation manifests, Rust operation planners, backup/restore or bounded export plans, and plan-only file/cloud execution wording all line up for the secondary families. This remains contract parity; adapter-owned file pickers, cloud storage workflows, restore execution, and live fixture validation remain residual risk.

Search, wide-column, local/document/cache/analytics, time-series, and graph engines now have their Wave 17 object-view parity slice: completeness criteria, descriptor-backed workflows, focused descriptor tests, workspace routing, posture panels, and guarded action strips all line up for the secondary families. This remains contract parity; rendered live payload depth, plugin/capability-specific views, cloud permission payloads, and fixture-backed UI validation remain residual risk.

The current strongest live/native areas are MongoDB, PostgreSQL, CockroachDB, SQL Server, MySQL, MariaDB, Redis, SQLite, Valkey, TimescaleDB, Oracle, DynamoDB, Elasticsearch, OpenSearch, DuckDB, and the shared result workbench. These engines are native-complete only for their documented scoped claims; provider-specific auth, broader admin execution, destructive maintenance, file/cloud import-export, snapshot/restore execution, and optional fixture/cloud variants remain outside those claims unless explicitly promoted. The full all-engine matrix remains at least contract-complete; the weakest remaining areas are live depth, optional fixture/cloud validation, and native management flows for cloud, graph, warehouse, and some secondary engines.

Strictly, MongoDB, PostgreSQL, CockroachDB, SQL Server/Azure SQL, MySQL, MariaDB, Redis, SQLite, Valkey, TimescaleDB, Oracle, DynamoDB, Elasticsearch, OpenSearch, and DuckDB are native-complete for their scoped claims. TimescaleDB's PostgreSQL-wire base, Timescale-specific profile/capability contract, richer live metadata normalizers, rendered dashboard object views, Toolkit/time-bucket diagnostics, before/after row-edit evidence plans, native guarded policy/job/import/export/backup previews, explicit plan-only policy/job/file execution boundary, and live-run optional fixture validator for extension/catalog, row-evidence, restricted-role, continuous-aggregate, policy/job boundary, compressed chunk, aggregate lag, Toolkit variant, bounded file-copy, and failed-job diagnostic evidence are complete for the scoped claim. Oracle is native-complete for its scoped SQLPlus-backed SQL workflow with typed connection/profile options, guarded live SELECT/EXPLAIN/DBMS_XPLAN execution, primary-key/ROWID row edits with bounded before/after evidence, SQL Developer-style trees, Oracle-specific IntelliSense, object views, diagnostics/storage/security contracts, guarded SQLPlus/Data Pump/RMAN previews, and optional fixture evidence; thin/OCI drivers, Data Pump/RMAN execution, broader admin execution, wallet/proxy/role fixture variants, and richer SQL Monitor rendering remain optional extensions. Search engines are native-complete only for their scoped plain-HTTP claims; cloud credentials, SQL plugin execution, Performance Analyzer dashboards, file/cloud import-export, snapshot execution, and broader admin execution remain optional extensions. DuckDB is native-complete for scoped local-file analytics with bundled local-file read/EXPLAIN/profile/catalog execution, guarded CSV table export/import, CSV backup-folder execution, database-file preflight/read-only and scoped lock-boundary evidence, fail-closed JSON/Parquet preloaded-extension-only boundaries, restore-package preflight, and explicit restore/admin/extension execution-boundary exclusions; extension-loaded JSON/Parquet execution and broader local OLAP mutation/admin/extension execution remain optional extensions. Remaining secondary/cloud engines still need more live adapter behavior before they should lose beta/foundation framing.

The quality gate now enforces several production-native guardrails:

- every completeness profile must cover all native-completion criteria and explicitly name remaining work for non-strong areas
- every declared engine must be listed in the contract-complete acceptance set, expose a completion claim, and keep per-criterion contract evidence
- secondary connection-flow contracts must keep shared types, right-drawer fields, browser validation, Rust models, interpolation, timeout routing, and redaction in sync
- secondary object-view contracts must keep descriptor-backed workflows, focused descriptor tests, workspace routing, posture panels, guarded action strips, and live-depth caveats in sync
- secondary guarded-operation contracts must keep object-view actions, browser manifests/planners, Rust manifests/planners, disabled reasons, confirmation requirements, and plan-only warnings in sync
- secondary diagnostics/performance contracts must keep diagnostics roots, object-view posture panels, browser diagnostics/profile payloads, browser and Rust metrics/profile planners, and live-sampling caveats in sync
- secondary import/export contracts must keep object-view actions, browser manifests/planners, Rust manifests/planners, backup/restore or bounded export request shapes, and plan-only file/cloud execution caveats in sync
- production query starters and structural tree manifests must not invent fixture/default objects such as `products`, `catalog`, or synthetic table names
- object-view menu labels must be workflow-specific instead of generic `Open View`
- tree manifests must continue avoiding fake/sample leaves and hidden preview-only clutter

## 2026-06-03 Native UX Audit Notes

The current application has a purpose-built object-view workspace for every declared datastore family, so the remaining work is depth and honesty rather than first-time wiring. The following findings should guide the next implementation slices:

- **MongoDB, PostgreSQL, CockroachDB, SQL Server, MySQL, MariaDB, Redis, SQLite, Valkey, TimescaleDB, Oracle, DynamoDB, Elasticsearch, OpenSearch, and DuckDB are native-complete scoped references.** MongoDB covers the scoped document workflow, including live guarded document insert/replace/delete, field edits, collection file import/export, adapter-driven fixture evidence, and optional permission/management fixture validation. PostgreSQL covers the scoped SQL workflow, including typed native connection/profile options, live SQL, row evidence, PostgreSQL-aware IntelliSense, extension/security/diagnostics object views, guarded routine/session/role/extension operations, rendered explain/profile dashboards, guarded import/export, bounded logical backup packages, and optional fixture validation with full `pg_dump`/`pg_restore` execution outside the claim. SQL Server covers the scoped SQL Server/Azure SQL workflow, including typed connection modes, live TDS SQL, primary-key row edits, SHOWPLAN renderers, Query Store, Extended Events, Agent, security/storage/runtime DMV views, guarded table import/export, bounded logical backup package execution, and restore-package validation with native `.bak`, bcp/sqlcmd, identity insert, and broader live admin execution outside the claim. MySQL and MariaDB cover their scoped SQL workflows with live SQL/edit bases, native connection/profile metadata, dialect-aware trees/query helpers, object views, diagnostics, guarded table import/export, bounded logical backup packages, and restore-package validation. Redis and Valkey cover scoped key workflows with guarded core/module-aware Redis behavior and core Valkey-compatible behavior. SQLite covers the scoped local-file SQL workflow. TimescaleDB covers the scoped PostgreSQL-wire time-series SQL workflow, Oracle covers the scoped SQLPlus-backed SQL workflow, DynamoDB covers the scoped local/endpoint-override plus opt-in AWS validation workflow, search covers scoped plain-HTTP Elasticsearch/OpenSearch workflows, and DuckDB covers scoped local-file analytics with guarded CSV file workflows and explicit restore/admin/extension exclusions. The rest of the matrix remains contract-complete while live validation depth continues engine by engine.
- **Production flows must stay live-metadata-only.** Browser-preview fixtures and test-suite templates still contain deterministic names such as `catalog`, `products`, `orders`, and `datapad_test_*`. Those are acceptable for tests/demo preview only; real desktop query creation, trees, and object views must not default to them.
- **Browser-preview fallback defaults are being retired before promotion.** Runtime helpers for MongoDB, Cosmos DB, DynamoDB, LiteDB, and Cassandra now fail closed for incomplete scoped targets instead of choosing fixture objects such as `catalog`, `products`, `Orders`, or `orders_by_customer`. Remaining preview fixtures should stay explicitly deterministic and never be used as fallback production targets.
- **MongoDB view result wording is now production-oriented.** View nodes expose `view-results` / `Results Preview` for bounded querying of real views; legacy `sample-results` is treated as a compatibility alias only.
- **Guarded plans are not the same as native management.** Many secondary engines now have good-looking operation chips and object-view cards, but the Rust paths still return preview-only or guarded request plans for writes/admin work. A datastore should not be considered native until safe live operations exist where users naturally expect them.
- **Capability hiding remains a release blocker.** Optional modules and managed-service features, such as Redis Stack, Atlas Search, OpenSearch security plugins, SQL Agent, Query Store, Oracle enterprise views, cloud IAM, and warehouse task/share surfaces, should only show when live metadata or a declared capability proves they are available.

## Readiness Matrix

| Datastore | Current readiness | What already exists | Remaining to be feature complete |
| --- | --- | --- | --- |
| MongoDB | Native-complete | Scoped document workflow: native/URI connections, database discovery, Mongo tree, find builder, raw commands, scripting, aggregation surface, aggregation-aware IntelliSense, document renderer, field inspector, efficiency mode, explain UI, metrics, profiler/currentOp/replica/shard/index-usage diagnostic profile payloads, object views, native guarded insert/replace/delete and field set/unset/rename/type-change edits, insert/upload views, guarded desktop collection import/export execution for JSON, Extended JSON, NDJSON, CSV, and BSON with parser/serializer tests, before/after summaries, adapter-driven fixture evidence, and optional permission/management fixture validation. | Optional extensions only: Atlas-only surfaces, live GridFS file workflows, replica/shard fixture depth, and live index/user/role/validator administration if those claims are later brought into scope. |
| Redis | Native-complete | Scoped Redis and Redis Stack key workflow: native key browser, Redis console, command-argument-aware IntelliSense with Redis Stack module hints and live COMMAND INFO ingestion from recent console results, scan filters, type summaries, key detail result view, metrics dashboard, Redis tree, stream consumer-group drilldowns with XINFO/XRANGE/XPENDING-backed Overview, Entries, Consumer Groups, Consumers, and Pending Entries views, Redis Stack module object views for RedisJSON, RedisTimeSeries, RedisBloom, RediSearch, and vector sets using optional read-only probes, deepened Redis object views for Pub/Sub, ACL/security, cluster, sentinel, Lua scripts, function libraries, slowlog, latency, memory, clients, INFO, and command stats, live guarded core key/member edits, guarded stream entry add/delete through `XADD`/`XDEL`, guarded RedisTimeSeries sample add/delete through `TS.ADD`/`TS.DEL`, guarded RedisJSON path edits through `JSON.SET`/`JSON.DEL`, guarded vector-set member/attribute edits through `VADD`/`VREM`/`VSETATTR`, guarded desktop JSON/NDJSON key import/export for strings, hashes, lists, sets, sorted sets, streams, whole-document RedisJSON, RedisTimeSeries samples, vector-set elements, and Redis DUMP/RESTORE snapshot envelopes for Bloom, Cuckoo, CMS, TopK, and t-digest module values, passing opt-in Redis Stack/Valkey fixture validation for core/module/snapshot paths, capability-hiding tests, and Redis Stack/module folders hidden until live metadata proves availability. | Optional extensions only: run `--require-vector` when the Redis Stack fixture image exposes `VADD`, and add cluster/Sentinel fixture evidence only if those topology claims are brought into scope. |
| Valkey | Native-complete | Core single-node Redis-compatible workflow: Redis-compatible connection flow, key browser, console, command-argument-aware IntelliSense, key/result model, diagnostics, live guarded core and stream entry edits, guarded desktop JSON/NDJSON key import/export execution for strings, hashes, lists, sets, sorted sets, and streams, Valkey-specific manifest/sidebar/browser-preview copy, reference test coverage, optional fixture validation for core key-file primitives, TTL behavior, permission-denied guarded writes, large key-file primitives, and stream groups, plus optional module hiding without Redis Stack/vector-only claims. | Optional extensions only: add cluster/Sentinel evidence if topology support enters the release claim, and finish module-aware edit/import-export parity only for confirmed Valkey-compatible modules. |
| PostgreSQL | Native-complete | Scoped SQL workflow: typed native connection/profile options with right-drawer fields, Rust interpolation, timeout routing, encoded DSN parameters, live SQL path, schema/table/security/diagnostics tree, extension folders with update hints, raw SQL default, SELECT builder for scoped objects, object descriptors, desktop live-scope tests for primary-key row edits, before/after row evidence metadata for desktop row edits, compact storage/index/security/extension/activity posture panels, role memberships, default privileges, normalized schema/table/routine/sequence grants, extension-owned objects, PostgreSQL-aware IntelliSense with `pg_catalog`/`information_schema` helpers, routine call/definition snippets, profile/session/lock/routine/extension snippets, guarded parameterized routine execution plans with signature-aware bindings, guarded backend cancel/terminate previews with PID and current-backend checks, rendered EXPLAIN plan payloads with table/raw fallbacks, live `pg_stat_database` metrics, `pg_stat_activity` session/wait/blocking profiles, `pg_locks` lock posture profiles, `pg_stat_user_tables` relation/vacuum/index-scan profiles, optional `pg_stat_statements` top-query profiles, rendered `EXPLAIN ANALYZE BUFFERS` JSON profile dashboards with operator stages and plan/table/raw fallbacks, guarded role grant/revoke and extension update/drop previews, guarded profile/index/grants/vacuum/analyze/reindex previews, guarded desktop CSV/JSON/NDJSON table export/import, bounded JSON/SQL logical backup execution, and optional fixture validation for seeded volume, catalogs, diagnostics, routines, profile primitives, row evidence, permission denial, import/export primitives, and bounded backup evidence. | Optional extensions only: full pg_dump/pg_restore execution, managed provider IAM/proxy/certificate combinations, generated/custom-type stress fixtures, richer privilege editors, and restore execution if those claims enter scope. |
| CockroachDB | Native-complete | Scoped distributed SQL workflow: PostgreSQL-wire live SQL and row edits, typed CockroachDB deployment/cloud/region/version profile metadata, auth/TLS disabled reasons, explicit capability gates, Cockroach-owned database/schema/cluster/security/diagnostics trees, CockroachDB SHOW/crdb_internal IntelliSense helpers, object descriptors and posture panels for jobs/ranges/regions/contention/security/zone surfaces, browser and Rust payload contracts for jobs, ranges, regions/localities, sessions, statements, transactions, contention, locks, statistics, certificates, zone configurations, and guarded jobs/ranges/regions/sessions/contention/role/default-privilege/zone/import/export/backup/restore planners with external-storage, permission, confirmation, read-only/environment, and scan/cost guardrails. | Optional extensions only: live Cockroach Cloud/capability probing, rendered EXPLAIN ANALYZE DEBUG dashboards, job-control execution, zone changes, and live destructive/data-movement execution. |
| SQL Server / Azure SQL | Native-complete | Scoped SQL Server/Azure SQL workflow: TDS query path, typed SQL Server/Azure SQL connection option validation, per-mode plan-only disabled reasons for Windows Integrated, Microsoft Entra password/integrated/interactive, managed identity, service principal, and certificate auth, SSMS-inspired tree, raw T-SQL default, object descriptors for databases/tables/procedures/functions/Query Store/Extended Events/Agent/security/storage/performance, compact storage/index/workload/Extended Events/security/Agent posture panels, runtime DMV profile payloads for cached query stats, active requests, waits, file I/O, memory grants, transactions, and missing-index impact, result grid, live primary-key row edits, rendered SHOWPLAN_TEXT explain payloads with table/raw fallbacks, rendered XML Showplan profile payloads with statement estimates and relational operator tables, Query Store status/options, top-query runtime, forced-plan, and regression payloads, database/server-scoped Extended Events session/event/target payloads with warnings, `msdb`-backed Agent service/job/schedule/alert/operator/proxy payloads with Azure/restricted-login warnings, security payloads for users/roles/memberships/schemas/permissions/certificates/keys/credentials/audits, storage payloads for files/filegroups/partition schemes/functions/boundaries/allocation units, guarded Update Statistics, index maintenance, Query Store workload previews, guarded desktop CSV/JSON/NDJSON table export/import, bounded JSON/SQL logical backup execution, and restore-package validation. | Optional extensions only: native `.bak` BACKUP/RESTORE, bcp/sqlcmd bulk workflows, identity insert, SQL Agent/Event Session/grant setting execution, and live fixture/cloud validation if those claims enter scope. |
| MySQL | Native-complete | Scoped MySQL workflow with typed native connection/profile options for TCP, Unix socket, Cloud SQL socket, managed metadata, auth-mode metadata, SSL modes, certificate paths, charset, collation, time zone, statement cache capacity, and connect/command timeouts; dialect-aware SQL SELECT builder, MySQL-specific IntelliSense keywords/functions, information_schema and performance_schema helpers, routine call/definition snippets, EXPLAIN FORMAT=JSON, optimizer trace, processlist/wait, statement digest, metadata-lock, and routine inventory snippets, backtick-aware alias completions, Workbench-style tree manifest, SQL editor, live primary-key row edits, shared relational result/edit contracts, storage/index/security/diagnostics posture panels, detailed statement digest/table-I/O/metadata-lock/optimizer-trace/status-counter sections, server-level users/roles/grants/sessions/status/slow-query/performance_schema/metadata-lock/optimizer-trace branches, rendered EXPLAIN payloads, live status/session/wait/statement-digest/table-I/O/InnoDB/optimizer diagnostics, browser-preview parity, structured guarded table maintenance, routine execution, event toggle, security inspection, and user account state previews with explicit privileges and disabled reasons, guarded desktop CSV/JSON/NDJSON table import/export, bounded JSON/SQL logical backup package execution, and restore-package validation. | Optional: cleartext/IAM auth, selected live admin execution, `LOAD DATA INFILE`, mysqlpump/mysqldump parity, richer grant editing, replication-channel depth, and full restore execution. |
| MariaDB | Native | Native-complete scoped MariaDB workflow with MySQL-compatible live SQL/edit base, typed MariaDB connection/profile metadata, MariaDB-aware Workbench-style tree branches, native MariaDB object-view descriptors and posture cards, SQL editor, live primary-key row edits, explicit `EXPLAIN FORMAT=JSON` and guarded `ANALYZE FORMAT=JSON` previews, MariaDB-specific IntelliSense helpers, status/version/storage-engine diagnostics, Aria metrics, role-mapping security previews, preview-first maintenance/routine/event/user workflows, guarded table import/export, bounded logical backup packages, and restore-package validation. | Optional extensions: selected live admin execution, `LOAD DATA INFILE`, mariadb-dump/mysql dump parity, richer role/grant editing, and full restore execution. |
| SQLite | Native-complete | Scoped local-file SQL workflow: local file open/create, SQLite tree, raw SQL and scoped SELECT builders, object descriptors, table results, local database fixture support, live primary-key row edits, rendered EXPLAIN QUERY PLAN/bytecode payloads, compact file/PRAGMA/schema posture panels, guarded index/check/analyze/optimize/vacuum/reindex previews, and guarded desktop live `VACUUM INTO` backup plus CSV/JSON/NDJSON table/view export and table import workflows. | Optional extensions only: add restore execution, encrypted-provider live builds, deeper trigger/index authoring, richer extension-module panels, and larger streaming fixtures if those claims enter scope. |
| Oracle | Native-complete, scoped SQLPlus SQL workflow | Oracle typed service/SID/TNS/Easy Connect/TCPS/cloud-wallet connection option model, SQLPlus runtime/path selector, SQL/PLSQL/explain descriptors, guarded SQLPlus live SELECT/EXPLAIN/DBMS_XPLAN execution when configured, primary-key/ROWID row edits with bounded before/after evidence, read-only and confirmation gates, Oracle-specific IntelliSense snippets and safe identifier quoting, SQL Developer-style tree, descriptor-backed object views for schema/table/package/security/storage/performance/diagnostics surfaces, DBMS_XPLAN-shaped plan/profile payloads, guarded SQLPlus/Data Pump import/export and RMAN backup/restore previews, diagnostics/storage/security contracts, and optional `fixtures:validate:oracle` evidence for seeded volume, dictionary/security/storage metadata, DBMS_XPLAN, SQL Monitor boundary, PL/SQL compile diagnostics, row identity primitives, bounded SQLPlus export/import, restricted dictionary denial, and Data Pump/RMAN preview boundaries. | Optional thin/OCI driver support, cancellation, cursor paging, wallet/proxy/role fixture variants, richer SQL Monitor rendering, broader admin execution, and Data Pump/RMAN execution stay outside the scoped claim until separately guarded. |
| TimescaleDB | Native-complete | Scoped TimescaleDB workflow: PostgreSQL-wire base, typed Timescale deployment/profile metadata, profile capability hiding with restricted warnings, SQL SELECT builder exposure, browser preview-only SQL row-edit planning, desktop live-scope tests for primary-key row edits, native hypertable/chunk/compression/retention/continuous-aggregate/job tree, richer live metadata normalizers, compact profile/time-series posture panels, rendered time-bucket, chunk-sizing, compression-coverage, aggregate-freshness, job-history, Toolkit availability, bucket-window, and time-bucket query-history dashboards, rendered PostgreSQL EXPLAIN payloads, Timescale-specific before/after row-edit evidence plans, object-view action strips, guarded compression-policy, retention-policy, aggregate-refresh, job-control, import/export, backup/restore, profile/index/grants previews with Timescale-native preflights, explicit plan-only policy/job/file execution boundaries, and live-run optional fixture validation for extension/catalog, row-evidence, restricted-role, continuous-aggregate, policy/job boundary, compressed chunk, aggregate lag, Toolkit variant, bounded file-copy, and failed-job diagnostic metadata. | Optional extensions: Timescale Cloud/service capability probing, Toolkit-installed and pg_stat_statements-disabled image variants, generated-column/trigger stress fixtures, and any separately guarded live policy/file executor. |
| Elasticsearch | Native-complete, scoped plain-HTTP search workflow | Typed HTTP/Elastic Cloud/API key/default-index/TLS/timeout connection option contract with explicit plan-only disabled reasons for HTTPS/cloud/token/TLS/SigV4 runtime paths, search tree, aggregation-aware Query DSL builder, bounded live search execution for plain HTTP none/basic profiles, search hits/aggregation/profile renderers, normalized profile-stage payloads, desktop/Rust tests for explicit-id document edits with before/after `_doc` evidence, object descriptors for cluster/index/mapping/security/diagnostics, native cluster/field/shard/segment/lifecycle/ingestion/security/slow-log/allocation panels, guarded HTTP-shaped profile/explain/force-merge/cache/reindex/open-close/mapping/settings/alias/data-stream/template/pipeline/lifecycle/task/security/bulk/snapshot/restore/slow-log/allocation previews with preview-first execution gates, and optional `fixtures:validate:search` evidence for seeded volume, mappings, aggregation/profile payloads, document evidence, diagnostics, and bounded `_search` export plus `_bulk` import primitives. | Optional extensions: production cloud auth, ES\|QL, desktop file/cloud import-export, snapshot execution, and broader live admin execution remain outside the scoped claim until separately guarded. |
| OpenSearch | Native-complete, scoped plain-HTTP search workflow | Typed HTTP/managed endpoint/AWS SigV4/default-index/TLS/timeout connection option contract with explicit plan-only disabled reasons for managed/HTTPS/token/TLS/SigV4 runtime paths, search tree, aggregation-aware Query DSL builder, bounded live search execution for plain HTTP none/basic profiles, search hits/aggregation/profile renderers, normalized profile-stage payloads, desktop/Rust tests for explicit-id document edits with before/after `_doc` evidence, object descriptors for cluster/index/mapping/security/diagnostics, native cluster/field/shard/segment/lifecycle/ingestion/security/slow-log/allocation panels, OpenSearch security and ISM-aware request routing, guarded HTTP-shaped profile/explain/force-merge/cache/reindex/open-close/mapping/settings/alias/data-stream/template/pipeline/lifecycle/task/security/bulk/snapshot/restore/slow-log/allocation previews with preview-first execution gates, and optional `fixtures:validate:search` evidence for seeded OpenSearch volume, mappings, aggregation/profile payloads, document evidence, diagnostics, bounded `_search` export plus `_bulk` import primitives, and OpenSearch SQL, ISM, security, and Performance Analyzer boundary evidence. | Optional extensions: managed SigV4/IAM runtime execution, OpenSearch SQL plugin execution, Performance Analyzer dashboards, desktop file/cloud import-export, snapshot execution, and broader live admin execution remain outside the scoped claim until separately guarded. |
| DynamoDB | Native-complete, scoped local/endpoint-override plus opt-in AWS validation workflow | Key-condition builder, projection/filter and conditional-write expression helpers, guarded read-only PartiQL `ExecuteStatement`, typed local/AWS connection option UI and contract, endpointUrl-aware read API execution, SigV4-shaped local/endpoint-override JSON API headers, table tree, desktop live-scope tests for guarded item put/update/delete with before/after evidence, capacity/cost/profile result shapes, DescribeLimits diagnostics, credential-mode disabled reasons, object descriptors, native key-design/capacity/TTL/streams/backup/index panels, guarded object-view previews for CloudWatch metrics, capacity updates, TTL, streams, GSI create/delete, IAM-style access inspection, table backup/restore, table export/import, table deletion, optional `fixtures:validate:dynamodb` evidence for DynamoDB Local seeded volume, read/query capacity, conditional item edits, transient key/GSI/TTL metadata, and backup/import-export boundaries, plus opt-in `fixtures:validate:dynamodb:cloud` AWS evidence for STS identity, ListTables, DescribeLimits, table diagnostics, CloudWatch metrics, IAM simulation, STS AssumeRole, web identity, ECS task credentials, and EC2 metadata credentials. | Optional extensions: production AWS HTTPS SDK runtime execution, S3 import/export job execution, backup create/delete, restore execution, richer condition-expression UI, and deeper CloudWatch/IAM dashboards. |
| Cassandra | Foundation+ | CQL builder scaffolding, typed contact-point/TLS/policy connection option UI and contract, keyspace/table tree, object descriptors, partition-model/storage/cluster-health panels, request-builder execution with partition-key/tracing warnings, browser-preview payloads, guarded object-view previews for tracing, SAI/index create/drop, permission inspection, diagnostics metrics, cqlsh COPY import/export, nodetool snapshot/SSTable restore, and object drops. | Add native binary protocol depth, live metadata, rendered tracing timelines, repair dashboards, SAI/index guidance, partition-key-safe live edits, and optional nodetool/JMX-backed diagnostics. |
| Cosmos DB | Foundation+ | Typed emulator/account-endpoint/Entra/managed-identity/resource-token connection options, account/container tree, document result contracts, partition/RU/indexing/global distribution panels, and guarded throughput/consistency/failover/profile/index/access/export/drop previews. | Add signed Azure auth, SQL API builder, live throughput/change-feed diagnostics, guarded item/import/delete execution workflows, and Azure portal parity for backups/private networking. |
| LiteDB | Foundation+ | Local database model, create/open direction, collection/index/file-storage tree, object-view posture panels, guarded local health/checkpoint/compact/index/export/backup/drop previews, local-file read/write open preflight, encryption and lock-boundary metadata, configured sidecar read-dispatch contracts with deterministic fixture-token and local sidecar-process evidence, sidecar-only full-document CRUD planning, optional validator evidence, and opt-in .NET sidecar validation for collection listing, bounded reads, index metadata, guarded document CRUD, missing-file mapping, and redaction. | Complete encrypted local file handling, collection/index/file-storage management execution, import/export execution, packaged sidecar distribution, and exclusive writer-lock validation. |
| Memcached | Foundation+ | Typed server-list/protocol/SASL/timeout connection options, stats/slabs/items/settings tree, diagnostics descriptors, cache/slab/item posture panels, guarded stats/settings/reset/flush/metadata dump previews, and known-key get/gets/set/touch/incr/decr/delete previews. | Add live protocol/SASL execution, binary protocol coverage where useful, CAS-safe value editing, multi-node guardrails, and richer dashboard sampling. |
| DuckDB | Native-complete, scoped local-file analytics workflow | Local OLAP tree, typed local-file/memory/extension/thread/memory-limit connection options, SQL SELECT builder coverage with generic mutation scopes intentionally hidden, bundled local-file read SQL execution, bounded result payloads, rendered EXPLAIN and EXPLAIN ANALYZE profile payloads, optional `fixtures:validate:duckdb` evidence for temporary `.duckdb` connection/catalog/table inspection/read/profile/diagnostics/guard boundaries, deterministic DuckDB IntelliSense, object descriptors for schemas/tables/extensions/files/pragmas, local-file/file-analytics/extension/maintenance posture panels, structured analyze/checkpoint/object admin-scope previews with lock, rollback, identity, read-only promotion gates, and explicit `adminExecutionBoundary` scoped-exclusion metadata, structured extension install/load previews with catalog, controlled-directory, network, installed-state, native-code gates, and explicit `extensionExecutionBoundary` scoped-exclusion metadata, guarded live CSV table export/import plus CSV backup-folder execution through the desktop operation workflow, database-file preflight metadata, read/open/write probes, `lockBoundary` metadata for scoped file-workflow validation and external-process contention residuals, blocked read-only disk guard evidence, `formatPreflight` metadata with preloaded-extension-only JSON/Parquet execution boundaries, controlled extension-directory setup, fail-closed JSON/Parquet unloaded-extension evidence, restore-package preflight for `schema.sql`, `load.sql`, file counts, bytes, detected formats, and target write/open readiness, and `restoreExecutionBoundary` metadata that explicitly scopes destructive `IMPORT DATABASE` execution out of the native claim. | Optional extensions only: richer PRAGMA/profile timelines, extension-loaded JSON/Parquet execution, external-process lock stress fixtures, and local OLAP mutation/admin/extension execution if scoped boundaries become live with fixture evidence. |
| ClickHouse | Foundation+ | Warehouse tree, typed HTTP/native endpoint/database/auth/TLS/row-limit connection options, SQL SELECT builder coverage with no edit scopes, SQL/HTTP-oriented contract, object descriptors, query/log/metrics surfaces, cost/compute/storage/access posture panels, ClickHouse query-log/MergeTree/cluster/maintenance posture panels, and guarded EXPLAIN/profile/metrics/access/export/optimize/TTL/freeze/drop previews. | Add robust native protocol behavior, rendered system.query_log dashboards, EXPLAIN pipeline UI, live cluster metrics, live import/export, and guarded execution for maintenance workflows. |
| Snowflake | Foundation+ | Cloud warehouse contract, typed account/endpoint/database/schema/warehouse/role/auth/TLS/row/cost connection model, SQL SELECT builder coverage with no edit scopes, warehouse/stage/job/security tree, object descriptors, query-history, credit, warehouse-load, stream/share posture panels, and guarded explain/cost/metrics/access/export/clone/suspend/resume/drop previews. | Add live OAuth/programmatic auth modes, role/warehouse selectors, rendered query profile graph, live utilization/history queries, import/export execution, and task/stream/share management execution. |
| BigQuery | Foundation+ | Cloud warehouse contract, typed endpoint/project/dataset/location/auth/row/cost connection options, SQL SELECT builder coverage with no edit scopes, project/dataset/table/job tree, dry-run/cost model, object descriptors, job-timeline, reservation, slot-usage, table-storage, IAM posture panels, and guarded dry-run/IAM/metrics/export/copy previews. | Add ADC/service account auth execution, INFORMATION_SCHEMA live metrics, IAM execution, scheduled query management, dry-run-first workflow, export/import jobs. |
| Prometheus | Foundation+ | PromQL language, typed endpoint/auth/path-prefix/tenant/range connection options, metric/label/target/rule tree, pinned PromQL range-builder descriptor with no edit scopes, chart/series result contracts, object descriptors, posture panels, native profile, metrics/stats, and cardinality operation previews. | Add native PromQL builder depth, deeper targets/rules/alerts/TSDB live views, range chart polish, optional live profile/cardinality execution. |
| InfluxDB | Foundation+ | Typed v1/v2/v3 endpoint/auth/org/bucket/database/retention/query-language connection options, bucket/measurement/tag/field tree, pinned Flux/InfluxQL builder descriptor with no edit scopes, time-series result contracts, object workspace foundation, posture panels, native profile/metrics/access/export/delete and retention update previews. | Add deeper token/task/retention management views, cardinality diagnostics, line protocol import execution, guarded live bucket/task workflows. |
| OpenTSDB | Foundation+ | Typed endpoint/auth/path-prefix/default-metric connection options, metric/tag/aggregator/downsample tree, pinned metric-query builder descriptor with no edit scopes, series result contract, object descriptors, posture panels, native stats/export/delete and UID repair metadata operation previews. | Add metric/tag query builder depth, UID/tree management execution, stats dashboards, HBase/backend health guidance, live API export/import flows. |
| Neo4j | Foundation+ | Graph family contracts, typed endpoint/database/auth/TLS/query options, pinned Cypher pattern-builder descriptor with no edit scopes, tree/object descriptors, graph renderer shape, guarded EXPLAIN/PROFILE, JMX metrics, index/security/constraint/drop, and graph export previews. | Add live Bolt/Cypher depth, richer graph visualization, rendered EXPLAIN/PROFILE operator graphs, full schema/index/constraints/security execution, safe graph operations. |
| ArangoDB | Foundation+ | Graph/document tree foundation, typed endpoint/database/graph/auth/TLS/query options, pinned AQL graph-builder descriptor with no edit scopes, graph workspace descriptors, guarded AQL explain/profile, metrics, security, index, drop, and export previews. | Add deeper live HTTP/AQL execution, collections/graphs/indexes, graph views, rendered explain/profile, Foxx/security execution later. |
| JanusGraph | Foundation+ | Gremlin/graph contract foundation, typed endpoint/traversal-source/auth/TLS/query options, pinned Gremlin traversal-builder descriptor with no edit scopes, descriptor routing, traversal explain/profile, management metrics, index lifecycle, backend-health, and graph export previews. | Add live Gremlin depth, schema/index/property views, backend health, reindex operation execution previews, and optional backend/index-service fixtures. |
| Neptune | Foundation+ | Graph cloud contract foundation, typed endpoint/path/database/language/AWS SigV4/IAM/TLS options, pinned Gremlin/openCypher builder descriptor with no edit scopes, loader-job concepts, and guarded Gremlin explain/profile, CloudWatch, IAM access, and import/export previews. | Add Gremlin/openCypher/SPARQL mode switching, live IAM/SigV4, loader jobs, rendered explain/profile, CloudWatch metrics dashboards. |

## Cross-Cutting Work Still Required

These items affect many engines and should be finished before declaring the platform production-ready:

1. Live depth: replace remaining browser-preview or plan-only paths with real adapter calls where drivers are realistic.
2. Capability hiding: optional tree nodes must stay hidden until live metadata or capability checks prove they exist.
3. Object views: every visible tree item should open a purpose-built workspace, not a raw payload dump. Workflow actions should be adapter-owned, hidden when unavailable, and linked to a real query or view section.
4. Safe edits: enable live edits only for complete identities and keep read-only/environment guards centralized.
5. Diagnostics: each engine needs native explain/profile/metrics/history views, not just generic counters.
6. Import/export: move from result-level export to engine-native import/export and backup/restore workflows.
7. Connection forms: finish datastore-specific connection modes without bloating the generic drawer.
8. IntelliSense: deepen metadata-aware suggestions for routines, aliases, indexes, document paths, commands, and cloud objects.
9. Tests: add deterministic UI/browser-preview tests for every visible tree/object-view action and optional fixture/live tests for promoted engines.

## Completed Native-Completion Slices

- **2026-06-03: Wave 0 quality gate stabilization.** The current in-progress scoped-target/runtime slice now passes `npm run check:all` without raising module-size budgets. Oversized React state/runtime and Rust scoped-tab modules were split around command wrappers, save helpers, connection-health reduction, Redis scoped-tab helpers, and scoped-tab tests. A quality drift test now keeps datastore engine declarations synchronized across the shared engine list, roadmap backlog, tree manifests, Rust registry dispatch, and object-view descriptor ownership.
- **2026-06-03: No fake scoped targets.** Browser/runtime scoped target helpers now keep incomplete MongoDB, Cosmos DB, DynamoDB, LiteDB, and Cassandra targets empty or in inspect-only states instead of defaulting to fixture object names. Desktop scoped Mongo query creation has a matching regression so right-click/query flows cannot silently target `products` when the selected object is only a connection or malformed node.
- **2026-06-03: Preview capability hiding.** Browser-preview Redis now matches the shared manifest by showing core databases, Pub/Sub, Lua, ACL, and diagnostics while keeping optional Redis Functions/module surfaces out unless live metadata supplies them. SQL Server preview database folders now keep optional Agent/Extended Events surfaces out of the core database tree, and blank SQL Server profiles no longer invent a `datapadplusplus` database or `dbo.object` target.
- **2026-06-03: Wave 1 Redis/Valkey reference contracts.** Redis and Valkey runtime manifests now advertise the same key browser, TTL, guarded admin, ACL/permission, streaming, import/export, metrics, and structure capabilities that the shared roadmap exposes. Redis keeps vector-search capability; Valkey intentionally omits Redis Stack/vector-only claims. Native Redis database type folders now show core key types by default and add JSON, time-series, Bloom, search, or vector branches only after `MODULE LIST` detects those modules. Redis/Valkey operation manifests and generated previews now cover key export/import, rename, duplicate, move, expire/persist, and stream ack/delete plans.
- **2026-06-03: Wave 1 MongoDB diagnostics probes.** MongoDB adapter diagnostics now add optional profiler status, recent `system.profile` entries, `currentOp`, replica-set status, sharding state, and collection-scoped `$indexStats` probes. These remain read-only and permission-aware: unavailable probes add warnings rather than blocking the metrics already collected from `serverStatus` and `dbStats`.
- **2026-06-03: Wave 2 PostgreSQL rendered plan payloads.** PostgreSQL EXPLAIN execution now emits a shared `plan` renderer payload with normalized plan lines, original table rows, JSON metadata, metrics, and raw text fallback so the existing result workbench can render it consistently with MySQL, SQLite, and MongoDB plans.
- **2026-06-03: Wave 2 core SQL contract checkpoint.** SQL Server SHOWPLAN_TEXT now emits shared `plan` payloads with table/raw fallbacks, TimescaleDB now exposes PostgreSQL-wire live row edits plus import/export and backup/restore operation manifests, TimescaleDB operation plans now use chunk-aware export and pg_dump/pg_restore previews, and Oracle generated operation requests now include SQLcl/Data Pump import/export plus RMAN backup/restore previews while staying contract-only for live execution.
- **2026-06-05: SQL Server XML Showplan slice.** SQL Server profile planning now uses guarded `SHOWPLAN_XML` requests, and the desktop adapter extracts XML Showplan statement estimates plus relational operators into shared plan-renderer lines, operator tables, structured metadata, and raw XML audit samples while keeping `SHOWPLAN_TEXT` explain behavior intact.
- **2026-06-05: SQL Server Query Store depth.** SQL Server object inspections now collect Query Store status/options, top-query runtime rows, forced-plan rows, and recent-vs-prior regression hints, and the object-view workload panel renders those signals as Query Store state, top-query, forced-plan, and regression cards with focused row chips.
- **2026-06-05: SQL Server Extended Events depth.** SQL Server object inspections now collect database-scoped and server-scoped Extended Events sessions, event definitions, targets, running/stopped state, startup/retention/memory/causality settings, event/target counts, target data availability, and warnings, with compact insight cards plus shared detail sections for `extended-events` and `xevent-profiler` views.
- **2026-06-05: SQL Server Agent depth.** SQL Server object inspections now collect `msdb`-backed Agent jobs, schedules, alerts, operators, proxies, optional service status, last/next run signals, enabled/scheduled state, owners/categories, notification channels, proxy credential names, and Azure/restricted-login warnings, with compact Agent cards plus shared detail sections and workflow chips.
- **2026-06-05: SQL Server security/storage depth.** SQL Server object inspections now collect database users, roles, memberships, schemas, permissions, certificates, symmetric/asymmetric keys, database-scoped credentials, audit specifications, files, filegroups, partition schemes/functions/boundaries, and allocation-unit totals, with compact posture cards, focused workflow chips, descriptors, and shared detail sections.
- **2026-06-06: SQL Server Azure auth disabled reasons.** SQL Server/Azure SQL profiles now keep typed connection option metadata through runtime validation, the right drawer explains why Windows Integrated, Microsoft Entra password/integrated/interactive, managed identity, service principal, and certificate modes are plan-only, and the Rust TDS gate returns matching mode-specific disabled reasons before live execution.
- **2026-06-06: SQL Server runtime statistics profile depth.** SQL Server performance object views now collect cached statement runtime stats, active request/session rows, wait pressure, file I/O stalls, memory grants, active transaction log usage, and missing-index impact from native DMVs, with focused workload cards and browser/Rust preview requests aligned to those runtime surfaces.
- **2026-06-06: SQL Server guarded file workflows and graduation.** SQL Server now promotes scoped import/export and backup/restore manifests to guarded live support. Browser and Rust planners emit matching workflow contracts, and the desktop adapter executes bounded CSV/JSON/NDJSON table export/import plus JSON/SQL logical backup packages with concrete path, overwrite, read-only, row/table-limit, target-column validation, confirmation, and restore-package validation guardrails. SQL Server/Azure SQL is native-complete for the scoped claim while native `.bak` BACKUP/RESTORE, bcp/sqlcmd, identity insert, and broader live maintenance/admin execution stay optional.
- **2026-06-03: Wave 3 search and wide-column contract checkpoint.** Search operation manifests now cover mapping/settings, aliases, lifecycle explain, data-stream rollover, templates, pipeline simulation, bulk import/export, snapshot, and restore plans with parameterized and encoded HTTP request shapes. DynamoDB manifests now cover capacity, TTL, streams, backup create/restore, generic backup/restore, and import/export plans; Cassandra now advertises import/export plus backup/restore and generates cqlsh COPY, nodetool snapshot, and SSTable restore plans while keeping live CQL execution as the explicit residual risk.
- **2026-06-03: Wave 4 local/document/cache/analytics contract checkpoint.** Cosmos DB and LiteDB now expose document-native operation manifests for throughput, consistency, regional failover, checkpoint, compact, index rebuild, backup, import/export, metrics, access, and destructive plans. Memcached now exposes stats reset, flush, CAS reads, set/touch/increment/decrement/delete, metrics, and LRU dump previews. DuckDB, ClickHouse, Snowflake, and BigQuery are promoted to Wave 4 contract-complete native UX in the shared completeness matrix with explicit live/cloud/local validation residual risks.
- **2026-06-03: Wave 5 time-series/graph contract checkpoint.** Prometheus, InfluxDB, and OpenTSDB now expose time-series-native operation manifests for cardinality, retention, UID repair, metrics, access, import/export, and guarded destructive plans. Neo4j, ArangoDB, JanusGraph, and Neptune now expose graph explain/profile, metrics, access/IAM, index, graph export/import, and destructive previews with browser and Rust request-shape parity. All seven Wave 5 engines are promoted to contract-complete native UX in the shared completeness matrix with explicit fixture/live validation residual risks.
- **2026-06-03: Final all-engine contract acceptance.** The shared completeness matrix now separates native readiness from the contract-complete release claim. All 29 declared engines are listed in `CONTRACT_COMPLETE_DATASTORE_ENGINES`, every criterion carries `contractStatus`, `evidence`, and `contractNote` fields, browser datastore experiences expose the claim, and the quality gate compares the acceptance set to the declared engine registry so future datastore additions cannot drift silently.
- **2026-06-03: Wave 6 reference-engine hardening.** MongoDB safe editing is now marked strong in the shared matrix because the desktop adapter supports guarded live insert-document and field set/unset/rename/type-change edits when identity and environment checks pass. Redis and Valkey reference tests now pin live native key/member edit scopes on the desktop adapter while browser-preview experiences remain explicitly plan-only. MongoDB import/export wording now reflects collection-level native plans without implying live file execution.
- **2026-06-04: Redis/Valkey command-argument IntelliSense.** Redis and Valkey completions now use a dedicated provider with read command syntax, key-position detection, namespace prefixes, subcommands, SCAN options, range/count/path literals, INFO sections, and live command metadata fallback. The shared matrix now marks Redis/Valkey IntelliSense strong.
- **2026-06-04: Redis guarded core key file workflows.** Desktop Redis key import/export now runs guarded JSON/NDJSON workflows for strings, hashes, lists, sets, sorted sets, and streams with concrete path checks, overwrite/read-only/confirmation guards, create-only/replace/validate modes, TTL preservation, serializer/parser tests, and before/after metadata summaries. At that point, Valkey stayed contract-covered and fixture-gated until the later live key-file promotion.
- **2026-06-04: Redis object-view depth.** Redis object views now render Redis-specific Pub/Sub channel/pattern/subscriber descriptors, native slowlog, latency, memory, client, INFO, command-stat, ACL/security, cluster, sentinel, Lua script, and function-library payloads. The shared matrix now marks Redis object views strong while keeping optional fixtures as residual work.
- **2026-06-04: Redis live COMMAND INFO ingestion.** Redis console `COMMAND` and `COMMAND INFO` results now expose normalized command metadata tables and JSON `commandMetadata`; the IntelliSense catalog ingests that metadata from recent Redis/Valkey result payloads, filters write commands from suggestions, and uses live key-position hints for read commands. Module-backed workflows and optional fixtures remain.
- **2026-06-04: Redis stream consumer-group drilldowns.** Redis stream keys now expand into read-only Overview, Recent Entries, Consumer Groups, Consumer Group, Consumers, and Pending Entries object views backed by `XINFO STREAM`, `XRANGE`, `XINFO GROUPS`, `XINFO CONSUMERS`, and `XPENDING` payloads. Encoded stream node IDs keep colon-heavy keys and group names inspectable, and focused normalizer/sidebar tests pin the contract.
- **2026-06-04: Redis Stack module object views.** Redis module folders and key inspections now render native RedisJSON, RedisTimeSeries, RedisBloom, RediSearch, and vector-set panels with optional read-only probes and explicit disabled reasons for module-backed import/export paths before guarded serializers are added.
- **2026-06-04: Redis Stack module command hints.** Redis IntelliSense now includes read-oriented RedisJSON, RedisTimeSeries, RedisBloom, RediSearch, and vector-set command docs plus module-aware argument suggestions for JSON paths, TimeSeries filters/ranges, RediSearch indexes/queries/options, probabilistic keys, and vector similarity modes. Valkey keeps Redis Stack static hints hidden unless live command metadata proves support.
- **2026-06-04: RedisJSON key file workflows.** Desktop Redis key import/export now includes whole-document RedisJSON values in the guarded JSON/NDJSON file workflow using `JSON.GET` and `JSON.SET key $`; Valkey and the then-remaining module serializers stayed fixture-gated until later slices separated Valkey core types from Redis Stack-only formats.
- **2026-06-04: RedisJSON path editors.** Redis key detail JSON trees now build path-specific `json-set-path` and `json-delete-path` requests; desktop live execution runs `JSON.SET`/`JSON.DEL` only for concrete Redis JSON keys after read-only, type, and confirmation checks, and Valkey hides RedisJSON edit scopes until compatible live evidence exists.
- **2026-06-04: Redis stream entry editors.** Redis and Valkey now advertise guarded `stream-add-entry` and `stream-delete-entry` edit scopes. Browser and desktop planners emit `XADD`/`XDEL` previews, desktop live execution verifies an existing stream key before running, and key inspection samples show stream entries as entry-level rows with guarded delete actions.
- **2026-06-04: RedisTimeSeries sample editors.** Redis now advertises guarded `timeseries-add-sample` and `timeseries-delete-sample` edit scopes. Browser and desktop planners emit `TS.ADD`/`TS.DEL` previews, desktop live execution verifies an existing TimeSeries key before running, key inspection samples show timestamp rows with guarded delete actions, and Valkey keeps RedisTimeSeries edits hidden.
- **2026-06-04: Redis vector-set editors.** Redis now advertises guarded `vector-add-member`, `vector-remove-member`, and `vector-set-attributes` edit scopes. Browser and desktop planners emit `VADD`/`VREM`/`VSETATTR` previews, desktop live execution verifies an existing vector-set key before running, key inspection samples show element rows with guarded delete actions, and Valkey keeps vector edit scopes hidden.
- **2026-06-04: Redis module key file serializers.** Desktop Redis key import/export now includes RedisTimeSeries sample files, vector-set element files, and Redis DUMP/RESTORE base64 snapshot envelopes for Bloom, Cuckoo, CMS, TopK, and t-digest module values, while Valkey keeps module file execution fixture-gated.
- **2026-06-04: Redis fixture evidence harness.** The optional fixture seed now creates Redis/Valkey stream consumer-group state, Redis Stack module keys, and vector-set keys when the image supports vector commands. `npm run fixtures:validate:redis -- --require-stack --require-valkey` validates Redis, Redis Stack, and Valkey evidence outside default CI.
- **2026-06-04: Redis fixture evidence run.** `redis-stack` and `cache` profiles were started, `fixtures:seed:all` completed after fixing MySQL/MariaDB line-item fixture joins, and `npm run fixtures:validate:redis -- --require-stack --require-valkey` passed for Redis core keys, stream groups, Redis Stack JSON/TimeSeries/probabilistic modules, DUMP/RESTORE module snapshot clones, Valkey keys, and Valkey stream groups. The current Redis Stack image does not expose `VADD`, so vector-set live fixture evidence remains image-dependent.
- **2026-06-04: Valkey live key file promotion.** Valkey key import/export manifests now advertise guarded desktop live execution for core Redis-compatible strings, hashes, lists, sets, sorted sets, and streams. Redis Stack module serializers stay hidden/plan-only for Valkey unless live metadata proves compatibility, and the optional Redis fixture validator now checks Valkey core export/import command primitives and TTL behavior.
- **2026-06-04: Valkey copy and disabled reasons.** Valkey shared/Rust tree manifests, sidebar fallbacks, browser preview explorer and structure responses, key-value delete copy, and module disabled-action payloads now use Valkey-specific wording, with Redis Stack/vector-only claims kept hidden unless compatible live evidence exists.
- **2026-06-04: Valkey permission and large key-file evidence.** The optional Redis fixture validator now proves Valkey guarded-write denial through a temporary read-only ACL user and validates large list/stream key-file command primitives while cleaning up all transient fixture keys.
- **2026-06-04: Redis native-complete graduation.** Redis is native-complete for the scoped Redis and Redis Stack key workflow. The matrix now treats vector-set live fixture validation as an explicit optional `--require-vector` path for Redis Stack images that expose `VADD`, while core Redis, stream groups, RedisJSON, RedisTimeSeries, probabilistic modules, DUMP/RESTORE snapshots, and Valkey evidence stay covered by the existing optional fixture validator.
- **2026-06-04: SQLite native-complete graduation.** SQLite is native-complete for the scoped local-file SQL workflow. Desktop SQLite now executes guarded `VACUUM INTO` backups, bounded table/view export to CSV/JSON/NDJSON, and CSV/JSON/NDJSON table import into explicit existing targets with read-only, absolute-path, overwrite, row-limit, validation-only, and confirmation guardrails. Restore execution, encrypted-provider builds, and deeper trigger/index authoring stay optional outside the claim.
- **2026-06-05: PostgreSQL diagnostics/profile slice.** PostgreSQL desktop diagnostics now return live session/wait/blocking, lock posture, relation/vacuum/index-scan, and optional `pg_stat_statements` top-query profile payloads with matching metrics and query-history evidence. Browser and Rust planners now emit a guarded `EXPLAIN (ANALYZE, BUFFERS, VERBOSE, FORMAT JSON)` request shape for profile operations while keeping execution preview-first.
- **2026-06-05: PostgreSQL role/grant and extension slice.** PostgreSQL explorer and object-view payloads now expose extension update hints, extension-owned objects, role memberships, default privileges, and normalized schema/table/routine/sequence permissions. Browser and Rust operation planners now cover guarded role grant/revoke plus extension update/drop previews, lifting PostgreSQL object-tree, object-view, guarded-operation, and test criteria to strong while import/export execution stays unfinished.
- **2026-06-05: PostgreSQL row-edit evidence slice.** PostgreSQL desktop row edits now execute insert/update/delete statements with `RETURNING *`; update/delete paths prefetch the target row by complete primary key before mutation, and execution metadata includes before/after row evidence for review.
- **2026-06-05: PostgreSQL guarded file workflows.** PostgreSQL desktop execution now covers bounded table/view export to CSV/JSON/NDJSON, CSV/JSON/NDJSON table import into explicit existing targets with target-column/type validation and validation-only mode, and bounded JSON/SQL logical backup packages with concrete path, overwrite, read-only, row/table-limit, and confirmation guardrails. Browser and Rust planners share the same workflow contracts, while full pg_dump/restore parity remains preview-first.
- **2026-06-05: PostgreSQL rendered profile dashboards.** PostgreSQL now accepts first-class `profile` execution for SELECT/WITH/VALUES statements, wraps the inner read with adapter-owned `EXPLAIN (ANALYZE, BUFFERS, VERBOSE, FORMAT JSON)`, renders operator stages through the profile renderer, preserves plan/table/raw fallbacks for auditability, and promotes guarded `postgresql.query.profile` execution to live after confirmation.
- **2026-06-05: PostgreSQL IntelliSense/query-helper slice.** PostgreSQL SQL completions now add dialect keywords/functions, `pg_catalog` and `information_schema` helper objects, routine call and definition snippets from cached explorer metadata, profile/session/lock/routine/extension snippets, and safe quoting for mixed-case or reserved identifiers. The shared matrix now marks PostgreSQL IntelliSense strong.
- **2026-06-05: PostgreSQL connection/profile options.** PostgreSQL and TimescaleDB drawer flows now expose typed PostgreSQL options for TCP, Unix socket, Cloud SQL proxy, managed metadata, application name, search path, target session attributes, TLS certificate paths, and connect/statement/lock/idle timeouts. Shared/Rust profiles preserve those options through interpolation, connection tests honor the timeout, and generated PostgreSQL DSNs encode the supported native parameters.
- **2026-06-05: PostgreSQL parameterized routine execution.** PostgreSQL function and procedure object views now expose a guarded Run action that carries routine kind, signature, return metadata, and parameterized bindings into browser and Rust operation planners. The shared matrix now marks PostgreSQL query-surface strong while keeping the overall claim contract-complete until optional fixture evidence, session/action guardrails, and the pg_dump/restore boundary are settled.
- **2026-06-05: PostgreSQL session/action guardrails.** PostgreSQL diagnostics and session object views now expose guarded Cancel and Terminate actions that generate `pg_cancel_backend` and `pg_terminate_backend` plans with explicit PID targeting, current-backend protection, permission warnings, and destructive transaction-impact wording. PostgreSQL remains contract-complete until optional fixture evidence and the pg_dump/restore boundary are settled.
- **2026-06-05: PostgreSQL native-complete graduation.** PostgreSQL is native-complete for the scoped SQL workflow. The optional fixture validator checks seeded relational volume, catalog/security/extension visibility, diagnostics and lock/session primitives, rendered profile/routine primitives, row-edit before/after evidence, table import/export primitives, bounded logical backup evidence, and permission-denied writes through a temporary read-only role; the local fixture run passed, with `pg_stat_statements` top-query evidence noted as optional because the default fixture does not install the extension. Full `pg_dump`/`pg_restore` execution, managed-provider IAM/proxy/certificate combinations, generated/custom-type stress fixtures, richer privilege editors, and restore execution remain optional extensions outside the claim.
- **2026-06-03: Wave 7 core SQL hardening.** PostgreSQL, CockroachDB, SQL Server/Azure SQL, MySQL, MariaDB, SQLite, and TimescaleDB marked safe editing and tests strong in the shared matrix because desktop manifests pinned live primary-key row-edit scopes while browser-preview manifests stayed plan-only. TimescaleDB also exposed the SQL SELECT builder and SQL-shaped row-edit preview guardrails despite its time-series family classification. Oracle had a separate row-edit residual risk until the later SQLPlus-backed edit graduation closed it.
- **2026-06-06: MySQL performance_schema diagnostics slice.** MySQL desktop diagnostics now collect `SHOW GLOBAL STATUS`, processlist waits, performance_schema statement digest, table/index I/O, metadata lock, InnoDB counter, and optimizer trace availability payloads; browser previews, tree manifests, operation planners, and object-view posture cards now expose the same status/performance_schema/optimizer surfaces.
- **2026-06-06: MySQL guarded file workflows.** MySQL now promotes scoped import/export and backup/restore manifests to guarded live support. Browser and Rust planners emit matching workflow contracts, and the desktop adapter executes bounded CSV/JSON/NDJSON table export/import plus JSON/SQL logical backup packages with concrete path, overwrite, read-only, row/table-limit, insertable-column validation, confirmation, and restore-package validation guardrails. `LOAD DATA INFILE`, mysqlpump/mysqldump parity, generated-column mapping controls, and restore execution stay optional.
- **2026-06-06: MySQL native management/action polish.** MySQL table maintenance, routine execution, event toggle, security inspection, and user account state operation previews now use structured browser/Rust workflow contracts with required privileges, confirmation/read-only guardrails, lock-impact, scheduler/definer/account warnings, explicit disabled reasons, and residual-risk wording. These operations stay preview-first; scoped file workflows remain the current live execution boundary.
- **2026-06-06: MySQL connection/profile polish.** MySQL shared profiles now carry typed native option bags for TCP, Unix socket, Cloud SQL socket, managed metadata, auth modes, SSL modes, certificate paths, charset, collation, time zone, statement cache capacity, and connect/command timeouts. The right drawer, browser validation, Rust interpolation, timeout routing, seed fixtures, SQLx DSN option mapping, and auth disabled reasons now stay aligned without storing secret values; cleartext plugin and IAM token auth remain plan-only until live support is validated.
- **2026-06-06: MySQL query-helper and IntelliSense polish.** MySQL now has dialect-aware SELECT builder output with backtick identifiers and numeric boolean literals, MySQL-specific IntelliSense keywords/functions, information_schema and performance_schema catalog helpers, routine call/definition snippets, EXPLAIN FORMAT=JSON, optimizer trace, processlist/wait, statement digest, metadata-lock, and routine inventory snippets, plus backtick-aware alias completions.
- **2026-06-06: MySQL object-view hardening and graduation.** MySQL diagnostic leaves now resolve to MySQL-native object-view descriptors, performance_schema/status/optimizer payloads render detailed statement digest, table/index I/O, metadata-lock, optimizer-trace, and status-counter sections with focused workflow chips, and diagnostics posture cards no longer treat status counters as storage metadata. MySQL is native-complete for the scoped claim; cleartext/IAM auth, selected live admin execution, `LOAD DATA INFILE`, mysqlpump/mysqldump parity, richer grant editing, replication-channel depth, and full restore execution stay optional.
- **2026-06-06: MariaDB query, IntelliSense, and diagnostics promotion.** MariaDB now has explicit `EXPLAIN FORMAT=JSON` and guarded `ANALYZE FORMAT=JSON` previews, MariaDB-specific IntelliSense helpers for status/version/storage engines and roles, diagnostics contracts for version variables, storage engines, role mappings, Aria counters, and `ANALYZE FORMAT=JSON` profile samples, plus MariaDB-labeled object-view posture cards. Connection flow, object tree, object-view polish, guarded-operation contracts, and import/export execution remained active at that point.
- **2026-06-06: MariaDB connection/profile and tree polish.** MariaDB now carries typed connection/profile metadata for managed-MariaDB mode, server flavor, SQL mode, default storage engine, charset/collation, LOCAL INFILE metadata, auth disabled reasons, validation, seed fixtures, and Rust interpolation. Shared TypeScript, Rust, browser-preview, and live explorer trees now expose MariaDB-aware role mappings, server variables, storage engines, and `ANALYZE FORMAT=JSON` diagnostics while hiding MySQL-only optimizer trace. The shared matrix now marks MariaDB connection flow and object tree strong; object-view polish, guarded operations, and import/export remained active at that point.
- **2026-06-06: MariaDB object-view polish.** MariaDB descriptors, relational section routing, browser-preview payloads, and object-view posture cards now render MariaDB role mappings, server variables, storage engines, `ANALYZE FORMAT=JSON` profile metadata, routines/events/security/diagnostics, and MariaDB-specific labels while hiding MySQL-only optimizer trace. The shared matrix marked MariaDB object views strong; guarded operations and import/export were the remaining active items at that point.
- **2026-06-06: MariaDB native-complete graduation.** MariaDB is native-complete for the scoped SQL workflow. Browser and Rust planners now emit MariaDB-specific guarded routine/event/admin/user previews, role-aware security inspection, live guarded `mariadb.data.import-export` and `mariadb.data.backup-restore` workflow contracts, and the desktop adapter routes MariaDB CSV/JSON/NDJSON table import/export, bounded JSON/SQL logical backup packages, and restore-package validation through the MySQL-family guarded file executor. Selected live admin execution, `LOAD DATA INFILE`, mariadb-dump/mysql dump parity, richer role/grant editing, and full restore execution remain optional.
- **2026-06-06: CockroachDB distributed native-completion slice.** CockroachDB now has a dedicated shared completion profile. Object tree, query surface, IntelliSense, object views, diagnostics/performance, safe editing, and tests are strong for the scoped distributed SQL slice: browser preview routes focused ranges, regions/localities, jobs, sessions, statements, transactions, locks, contention, statistics, grants, certificates, and zone-configuration nodes to CockroachDB-shaped payloads, Rust keeps live `crdb_internal` payload contracts, and IntelliSense adds CockroachDB SHOW helpers, `crdb_internal` objects, distributed explain/profile snippets, contention dashboards, and zone-configuration snippets. Connection/profile depth, guarded operations, and import/export remained active at that point.
- **2026-06-06: CockroachDB connection/profile capability hiding.** CockroachDB profiles now carry deployment mode, Cockroach Cloud organization/cluster metadata, cloud/default region, locality, version/build, auth/TLS disabled reasons, and explicit capability toggles for jobs, ranges, regions, cluster status/settings, sessions, contention, roles/grants, certificates, zone configurations, and `EXPLAIN ANALYZE`. The right drawer, browser validation, Rust interpolation, browser/Rust tree hiding, direct restricted inspection warnings, operation manifests, and object-view action strips now honor those capability gates. Guarded operations and import/export were still active before native-complete graduation.
- **2026-06-06: CockroachDB native-complete guarded workflow graduation.** CockroachDB now has browser and Rust planners for Cockroach-specific and generic jobs, ranges, regions, sessions, contention, role/grant/default-privilege, zone-configuration, `IMPORT`, `EXPORT`, `BACKUP`, and `RESTORE` workflows. Plans include external URI parameters, confirmation text, permission requirements, read-only/environment guard warnings, and scan/cost wording while keeping destructive/data-movement execution preview-first. CockroachDB is native-complete for the scoped distributed SQL claim; TimescaleDB becomes the active native-completion target.
- **2026-06-07: TimescaleDB connection/profile and capability checkpoint.** TimescaleDB profiles now carry deployment mode, project/service, region, extension schema/version, server version, license, policy disabled reasons, and capability toggles for hypertables, chunks, compression, retention, continuous aggregates, jobs, Toolkit, `EXPLAIN ANALYZE`, and live policy execution. Browser validation, right-drawer fields, Rust interpolation, connection-test extension warnings, restricted catalog payload warnings, rendered profile posture, and profile-specific policy disabled reasons are covered by deterministic tests. Dashboard/object-view depth, import/export/backup polish, optional fixture evidence, and guarded live policy execution were still active at that checkpoint.
- **2026-06-07: TimescaleDB dashboard/object-view checkpoint.** TimescaleDB browser payloads and object views now render time-bucket coverage, chunk sizing, compression coverage, continuous aggregate freshness, and job-history dashboards with hypertable and aggregate scoped filtering. Live metadata depth, import/export/backup polish, optional fixture evidence, before/after row-edit evidence, and guarded live policy execution were still active at that checkpoint.
- **2026-06-07: TimescaleDB guarded workflow/import-export checkpoint.** TimescaleDB browser and Rust planners now emit native compression, retention, continuous aggregate refresh, job-control, import/export, backup, and restore previews with hypertable/chunk/compression/job/continuous-aggregate/extension preflights, bounded time-window export clauses, JSON/NDJSON staging guidance, pg_dump/pg_restore review paths, object-view action strips, and preview-first disabled reasons. Live metadata depth, before/after row-edit evidence polish, optional fixture evidence, and explicit plan-only policy/file boundaries remained active.
- **2026-06-07: TimescaleDB live metadata and row-edit evidence checkpoint.** TimescaleDB Rust live inspections now normalize richer hypertable, chunk, compression, retention, continuous aggregate, job-stat, chunk-sizing, compression-coverage, aggregate-freshness, job-history, and job-reliability payloads. Browser and Rust row-edit plans now pin bounded primary-key prefetch plus `RETURNING *` evidence, while policy/file execution remains preview-first outside the scoped live row-edit claim.
- **2026-06-07: TimescaleDB optional fixture harness.** `npm run fixtures:validate:timescale` now validates extension/version visibility, seeded hypertables/chunks, metric volume, hypertable row-edit before/after evidence, restricted catalog visibility, permission-denied writes, continuous aggregate metadata, and retention-policy job metadata through transient `fixture_timescale_*` objects. The harness remains opt-in and does not execute background jobs or full file workflows.
- **2026-06-07: TimescaleDB Toolkit/time-bucket diagnostics.** Browser previews and Rust live inspections now expose Toolkit availability, visible `time_bucket`/`time_bucket_gapfill` functions, bounded bucket-window scan summaries, and optional `pg_stat_statements` time-bucket query-duration samples. Object views render those diagnostics as native Timescale sections instead of raw payloads.
- **2026-06-07: TimescaleDB scoped policy/file boundary.** Browser object views now expose backup previews for Timescale scopes, and browser/Rust planners stamp compression, retention, continuous aggregate refresh, job-control, import/export, backup, and restore plans with explicit plan-only execution-boundary preflights. The scoped claim keeps policy, job, and file execution preview-first while fixture evidence deepens.
- **2026-06-07: TimescaleDB expanded optional fixture evidence.** The `fixtures:validate:timescale` path now checks compressed chunk metadata, intentionally unrefreshed continuous-aggregate lag, Toolkit availability/time-bucket function variants, and bounded CSV export/import copy evidence in addition to extension/catalog, row-evidence, restricted-role, and policy/job boundary checks.
- **2026-06-07: TimescaleDB native-complete graduation.** The expanded `fixtures:validate:timescale` path passed against the seeded `sqlplus` fixture and now includes failed-job diagnostics through a transient scheduled job recorded in `job_errors`, `job_history`, and `job_stats`. TimescaleDB is native-complete for the scoped time-series SQL claim while policy/job/file execution remains preview-first unless a future guarded executor explicitly promotes it.
- **2026-06-07: Oracle fixture, IntelliSense, and contract-strength checkpoint.** Oracle now has Oracle-specific IntelliSense snippets for DBMS_XPLAN, SQL Monitor, sessions/waits, invalid objects, compile errors, package source, functions, procedures, packages, and safe identifier quoting. The optional `fixtures:validate:oracle` path passed against the seeded `oracle` profile and checks seeded relational volume, dictionary/security/storage metadata, DBMS_XPLAN output, SQL Monitor visibility or denial, PL/SQL source and compile diagnostics, row identity and DML `RETURNING` primitives, bounded SQLPlus export/import evidence, restricted dictionary denial, and Data Pump/RMAN preview-boundary wording through transient `fixture_oracle_*` objects. The shared matrix now marks Oracle connection flow, object tree, IntelliSense, object views, guarded operations, diagnostics/performance, import/export, and tests strong for the contract surface.
- **2026-06-07: Oracle SQLPlus live query slice.** Oracle connection options now include a contract-vs-SQLPlus runtime selector and SQLPlus path, Rust profile interpolation preserves those fields, connection tests report configured SQLPlus runtime status, and the desktop adapter can execute guarded live SQLPlus queries with a single-statement read guard, `/nolog` stdin credentials, password redaction, request timeout, bounded row wrapping, CSV normalization, and EXPLAIN PLAN/DBMS_XPLAN result handling. At that checkpoint, Oracle query-surface was strong while safe editing still needed adapter-backed row edits.
- **2026-06-07: Oracle native-complete graduation.** Oracle now has browser and Rust table row-edit scopes, Oracle-shaped browser preview requests, and a desktop SQLPlus row-edit executor for guarded insert/update/delete row workflows with primary-key or ROWID identity, bounded before/after evidence, read-only and confirmation gates, and explicit commit after evidence collection. Oracle is native-complete for the scoped SQLPlus-backed SQL workflow while thin/OCI drivers, Data Pump/RMAN execution, broader admin execution, wallet/proxy/role fixture variants, and richer SQL Monitor rendering remain optional extensions.
- **2026-06-07: DynamoDB conditional-write helper slice.** DynamoDB browser and Rust item-edit previews plus the desktop live edit adapter now emit deterministic condition expressions: update/delete require existing keyed items with `attribute_exists`, while put-item uses create-only `attribute_not_exists`. The request-shape tests pin expression-name merging, consumed-capacity requests, and before/after evidence plans.
- **2026-06-07: DynamoDB Local optional fixture evidence.** The new `fixtures:validate:dynamodb` path passed after starting and seeding the `cloud-contract` profile. It checks seeded DynamoDB Local volume, Scan/Query/GetItem consumed-capacity payloads, table/key/GSI/TTL metadata through transient `fixture_dynamodb_contract`, Query/GetItem/PartiQL reads, conditional item-edit before/after evidence, and local backup/import-export boundary behavior. DynamoDB Local omits ExecuteStatement consumed-capacity output and lacks cloud backup/export support in this image, so AWS IAM, CloudWatch, SigV4 credential modes, and S3 import/export execution remain residual cloud validation work.
- **2026-06-07: DynamoDB SigV4-shaped diagnostics.** The desktop DynamoDB JSON API path now uses typed `endpointUrl` before host/port and sends deterministic AWS4-HMAC-SHA256-shaped headers for local and endpoint-override validation. Diagnostics now include auth evidence, credential-mode disabled reasons, live/local `DescribeLimits` capacity signals when supported, CloudWatch and IAM request plans, and matching browser/Rust preview evidence. Full AWS SDK/HTTPS SigV4 credential-chain validation, live CloudWatch/IAM, S3 import/export, and managed backup validation remain optional cloud work.
- **2026-06-07: DynamoDB opt-in AWS cloud validator.** `fixtures:validate:dynamodb:cloud` now provides a live AWS evidence path outside default CI. It is gated by `DATAPADPLUSPLUS_DYNAMODB_CLOUD_VALIDATE=1`, resolves environment or shared-profile credentials, signs DynamoDB, STS, CloudWatch, and IAM requests with AWS4-HMAC-SHA256, and validates STS identity, ListTables, DescribeLimits, optional table diagnostics, optional CloudWatch metrics, and optional IAM simulation. Backup/create and S3 import/export execution remain preview-first.
- **2026-06-07: DynamoDB scoped native-complete graduation.** The optional AWS validator now covers environment/shared-profile credentials plus opt-in STS AssumeRole, AssumeRoleWithWebIdentity, ECS task credential, and EC2 metadata credential modes behind explicit provider and strictness variables. DynamoDB is native-complete for the scoped local/endpoint-override plus opt-in AWS validation workflow; production AWS HTTPS SDK runtime execution, S3 import/export job execution, backup create/delete, and restore execution remain optional extensions outside the claim.
- **2026-06-07: Elasticsearch query/profile and document evidence.** Elasticsearch/OpenSearch Query DSL builders now cover terms, date histogram, histogram, metric, and cardinality aggregations; desktop profile responses normalize rewrite/query/collector/aggregation timing into renderer-ready stages; and guarded explicit-id document edits now include before/after `_doc` evidence around live index/update/delete mutations.
- **2026-06-07: Elasticsearch validation boundaries and diagnostics.** Search live execution now has explicit plan-only disabled reasons for HTTPS, Elastic Cloud, managed OpenSearch, API-key/token, TLS/certificate, and AWS SigV4 profiles while keeping plain HTTP none/basic profiles live-capable. Browser/Rust operation manifests and planners now include preview-first execution gates for admin/import-export/snapshot workflows plus slow-log and allocation diagnostic plans, and object views render slow-log and allocation posture panels. At that checkpoint, Elasticsearch still needed selected admin/import-export execution or scoped exclusions, optional live/cloud fixture validation, and optional ES|QL support.
- **2026-06-07: Elasticsearch scoped native-complete graduation.** The new optional `fixtures:validate:search` path validates seeded Elasticsearch/OpenSearch volume, mappings, aggregation/profile payloads, explicit-id document evidence, slow-log/allocation diagnostics, and bounded `_search` export plus `_bulk` import primitives. Elasticsearch is native-complete for the scoped plain-HTTP workflow; production cloud auth, ES|QL, desktop file/cloud import-export, snapshot execution, and broader live admin execution remain optional extensions outside the claim. Active completion moves to OpenSearch.
- **2026-06-07: OpenSearch scoped native-complete graduation.** The optional `fixtures:validate:search` path now adds OpenSearch SQL, ISM, security, and Performance Analyzer boundary evidence to the shared search checks. OpenSearch is native-complete for the scoped plain-HTTP workflow while managed SigV4/IAM runtime execution, OpenSearch SQL plugin execution, Performance Analyzer dashboards, desktop file/cloud import-export, snapshot execution, and broader live admin execution remain optional extensions outside the claim. Active completion moves to DuckDB.
- **2026-06-07: DuckDB local-file evidence checkpoint.** `fixtures:validate:duckdb` now runs a focused bundled-runtime Rust adapter test against a temporary `.duckdb` file, validating connection tests, catalog roots, table inspection payloads, bounded read SQL, EXPLAIN, EXPLAIN ANALYZE profile payloads, diagnostics templates, write SQL guard failures, and plan-only file import boundaries. This checkpoint was the prerequisite for the guarded CSV file-workflow evidence added next.
- **2026-06-07: DuckDB guarded CSV file-workflow checkpoint.** `duckdb.data.import-export` and `duckdb.data.backup-restore` now have matching browser/Rust live-operation manifests, guarded workflow JSON planners, and desktop adapter execution for bounded CSV table export/import plus CSV `EXPORT DATABASE` backup folders. The optional DuckDB validator proves those paths without Docker alongside restore-preview and write-SQL guard boundaries. JSON/Parquet extension-backed gates, restore execution, extension execution, deeper cross-process lock evidence, and safe local OLAP mutation/admin workflows remain in progress.
- **2026-06-07: DuckDB database-file preflight/read-only guard checkpoint.** Live DuckDB table export/import and database backup workflows now return `databasePreflight` metadata for file/memory kind, path, parent/file existence, read-only state, read/write probes, and DuckDB open probes. Write workflows fail closed when the `.duckdb` file is read-only on disk, and `fixtures:validate:duckdb` proves both successful preflight metadata and the blocked read-only import response. JSON/Parquet extension-backed gates, restore execution, extension execution, deeper cross-process lock behavior, and safe local OLAP mutation/admin workflows remain in progress.
- **2026-06-07: DuckDB JSON/Parquet extension-format gate checkpoint.** DuckDB file-workflow planners and execution responses now carry `formatPreflight` metadata, prepare a controlled extension directory, probe `duckdb_extensions()`, and block JSON/Parquet operations when the required extension is not already loaded instead of auto-installing over the network. The optional DuckDB validator proves those extension-backed blocked paths without Docker or internet access. Preloaded/offline extension live validation, extension install/load execution, restore execution, deeper cross-process lock behavior, and safe local OLAP mutation/admin workflows remain in progress.
- **2026-06-07: DuckDB admin/extension execution-boundary checkpoint.** DuckDB admin and extension previews now carry explicit `adminExecutionBoundary` and `extensionExecutionBoundary` metadata in browser and Rust planners. Analyze/checkpoint/create/drop and extension install/load execution remain scoped out of the native claim until writer-lock, rollback/snapshot or offline-source trust, post-operation validation, explicit confirmation, and read-only promotion gates are live.
- **2026-06-07: DuckDB scoped lock-boundary checkpoint.** DuckDB desktop `databasePreflight` responses now include `lockBoundary` metadata for filesystem read/write open probes, DuckDB adapter open probes, scoped file-workflow validation, blocked read-only states, and explicit external-process contention residuals. Browser and Rust planners now emit matching `databaseLockBoundary` contracts for file import/export, backup, and restore preview workflows.
- **2026-06-07: DuckDB preloaded-extension boundary checkpoint.** DuckDB file-workflow planners and desktop `formatPreflight` responses now include `extensionExecutionBoundary` metadata. CSV is marked bundled-native, while JSON/Parquet are marked preloaded-extension-only, fail closed when the required extension is not loaded, and require offline source provenance plus extension-loaded fixture evidence before any broader live claim.
- **2026-06-07: DuckDB restore-package preflight checkpoint.** DuckDB restore previews now emit `restorePreflight` metadata after validating an absolute backup folder, `schema.sql`, `load.sql`, file counts, byte totals, detected backup formats, and target database write/open readiness. The optional DuckDB validator proves those markers against a generated CSV `EXPORT DATABASE` backup while destructive `IMPORT DATABASE` execution remains scoped out until rollback/snapshot, confirmation, and lock boundaries are native.
- **2026-06-07: DuckDB structured extension gate checkpoint.** DuckDB extension install/load previews now emit JSON contracts instead of bare SQL, including sanitized extension names, `duckdb_extensions()` catalog probes, controlled `extension_directory` expectations, no-network auto-install policy, installed-before-load checks, and native-code execution review gates. Browser and Rust manifests now expose explicit plan-only disabled reasons for extension installation/loading.
- **2026-06-07: DuckDB structured admin-scope checkpoint.** DuckDB table/database `ANALYZE`, checkpoint, and object create/drop previews now emit JSON contracts with admin-scope metadata, cross-process lock probe expectations, object identity/diff previews, rollback/backup review gates, read-only promotion checks, and explicit plan-only policy. Live admin/DDL execution remains scoped out until those gates are executable and fixture-backed.
- **2026-06-07: DuckDB restore execution-boundary checkpoint.** DuckDB restore planners and desktop restore preview responses now include `restoreExecutionBoundary` metadata with scoped-out destructive execution policy, preview validation state, blocked reasons, and required promotion gates for exclusive writer-lock evidence, snapshot/rollback artifacts, post-restore catalog validation, destructive confirmation, and read-only promotion blocking. The scoped claim now covers restore package validation and explicitly excludes live `IMPORT DATABASE` execution.
- **2026-06-07: DuckDB scoped native-complete graduation.** DuckDB is native-complete for the scoped local-file analytics workflow. Bundled local-file read/EXPLAIN/profile/catalog execution, guarded CSV table export/import, CSV backup-folder execution, database-file preflight/read-only and scoped lock-boundary evidence, fail-closed JSON/Parquet preloaded-extension-only boundaries, restore-package preflight, and explicit restore/admin/extension execution-boundary exclusions are covered by the matrix and optional validator. Extension-loaded JSON/Parquet execution and broader local OLAP mutation/admin/extension execution remain optional extensions outside the claim.
- **2026-06-07: LiteDB local-file boundary checkpoint.** LiteDB connection, diagnostics, explorer, query payloads, browser planners, and Rust planners now emit local-file preflight metadata, read/write open probes, password/encryption posture, explicit lock-boundary metadata, and `dotnet-litedb-sidecar` execution-boundary contracts. The optional `fixtures:validate:litedb` path proves the deterministic preflight contract without Docker or a .NET sidecar; live sidecar reads, document CRUD, encrypted-file validation, import/export execution, and exclusive writer-lock evidence remain in progress.
- **2026-06-07: LiteDB sidecar read-dispatch checkpoint.** LiteDB query execution now supports a configured `SidecarPath` option for guarded read-only sidecar envelopes, with deterministic fixture-sidecar coverage for bounded response normalization, timeout clamps, redacted failure output, and live-read boundary metadata. Real .NET sidecar binary validation, encrypted-file validation, document CRUD, import/export execution, and exclusive writer-lock evidence remain in progress.
- **2026-06-08: LiteDB local sidecar-process checkpoint.** LiteDB sidecar tests now compile and launch a temporary local executable through `SidecarPath`, validate the real stdin/stdout protocol path, report `local-sidecar-process` dispatch evidence, prove bounded read normalization through the spawned process, and verify process-reported file-open failures redact secrets.
- **2026-06-08: LiteDB .NET sidecar engine checkpoint.** A .NET LiteDB sidecar project now implements the stdin/stdout protocol with read-only collection, find/query, find-by-id, count, index, sample-schema, and explain surfaces, plus a fixture-seeding path gated by `DATAPADPLUSPLUS_LITEDB_SIDECAR_ALLOW_FIXTURE_SEED`. The opt-in `fixtures:validate:litedb:dotnet` validator creates a temporary real LiteDB database and validates collection listing, bounded reads, `$indexes` metadata, missing-file error mapping, and secret redaction. Encrypted-file validation, document CRUD, import/export execution, packaged sidecar distribution, and exclusive writer-lock validation remain in progress.
- **2026-06-08: LiteDB guarded document CRUD checkpoint.** LiteDB planning now exposes scoped full-document insert/update/delete, requires confirmation for every sidecar mutation, downgrades to plan-only when `SidecarPath` is missing, and emits `_id`-stable sidecar JSON with before/after evidence requests. The optional .NET sidecar validator now proves real insert/update/delete, before/after reads, read-only mutation blocking, `_id` mismatch blocking, and delete after-read absence against a temporary database. Encrypted-file validation, collection/file-storage management execution, import/export execution, packaged sidecar distribution, and exclusive writer-lock validation remain in progress.
- **2026-06-03: Wave 8 search and wide-column hardening.** Elasticsearch and OpenSearch now mark explicit-id document safe editing and tests as strong in the shared matrix, with desktop manifests pinning live index-document/update-document/delete-document scopes while browser-preview manifests remain plan-only. DynamoDB now has a separate profile from Cassandra so complete-key item put/update/delete editing and tests are strong for DynamoDB, while Cassandra keeps its contract-only live CQL row-edit residual risk.
- **2026-06-03: Wave 9 local/document/cache/analytics hardening.** Cosmos DB, LiteDB, and Memcached now mark deterministic Wave 4 test coverage as strong in the shared matrix while keeping edits and import/export execution preview-first. DuckDB, ClickHouse, Snowflake, and BigQuery now mark query surface and tests as strong because browser and desktop manifests pin SQL SELECT builders, and both manifests keep editable scopes empty until adapter-owned mutation safety exists.
- **2026-06-03: Wave 10 time-series and graph hardening.** Prometheus, InfluxDB, OpenTSDB, Neo4j, ArangoDB, JanusGraph, and Neptune now mark query surface and tests as strong because browser and desktop manifests pin time-series or graph query-builder descriptors, and both manifests keep editable scopes empty until adapter-owned mutation safety exists.
- **2026-06-03: Wave 11 deterministic IntelliSense hardening.** Search, DynamoDB/Cassandra, Cosmos DB, LiteDB, Memcached, DuckDB, ClickHouse, Snowflake, BigQuery, Prometheus, InfluxDB, OpenTSDB, Neo4j, ArangoDB, JanusGraph, and Neptune now mark IntelliSense as strong for the contract slice because browser providers cover deterministic keywords, objects, fields, commands, metrics, dimensions, graph schema concepts, and bounded snippets while leaving live metadata-aware suggestions as residual risk.
- **2026-06-03: Wave 12 secondary object-tree parity.** Search, DynamoDB/Cassandra, Cosmos DB, LiteDB, Memcached, DuckDB, ClickHouse, Snowflake, BigQuery, Prometheus, InfluxDB, OpenTSDB, Neo4j, ArangoDB, JanusGraph, and Neptune now mark object trees as strong for the contract slice because shared TypeScript manifests, Rust manifests, and browser explorer routing pin native roots and key sections without claiming live capability detection for every service mode.
- **2026-06-03: Wave 13 secondary connection-flow parity.** Search, DynamoDB/Cassandra, Cosmos DB, LiteDB, Memcached, DuckDB, ClickHouse, Snowflake, BigQuery, Prometheus, InfluxDB, OpenTSDB, Neo4j, ArangoDB, JanusGraph, and Neptune now mark connection flow as strong for the contract slice because shared option bags, right-drawer fields, browser validation, Rust models, interpolation, timeout selection, and redaction stay synchronized. Memcached now has a dedicated typed server-list/SASL/timeout option contract while live protocol/SASL validation remains residual risk.
- **2026-06-03: Wave 14 secondary guarded-operation parity.** Search, DynamoDB/Cassandra, Cosmos DB, LiteDB, Memcached, DuckDB, ClickHouse, Snowflake, BigQuery, Prometheus, InfluxDB, OpenTSDB, Neo4j, ArangoDB, JanusGraph, and Neptune now mark guarded operations as strong for the contract slice because object-view actions, browser manifests/planners, Rust manifests/planners, disabled reasons, confirmation requirements, and plan-only warnings stay synchronized. Live admin/write execution remains the residual risk.
- **2026-06-03: Wave 15 secondary diagnostics/performance parity.** Search, DynamoDB/Cassandra, Cosmos DB, LiteDB, Memcached, DuckDB, ClickHouse, Snowflake, BigQuery, Prometheus, InfluxDB, OpenTSDB, Neo4j, ArangoDB, JanusGraph, and Neptune now mark diagnostics/performance as strong for the contract slice because diagnostics tree roots, object-view posture panels, browser diagnostics/profile payloads, browser planners, Rust metrics/profile planners, and live-sampling caveats stay synchronized. Live telemetry, profile dashboards, and cloud/fixture sampling remain the residual risk.
- **2026-06-03: Wave 16 secondary import/export parity.** Search, DynamoDB/Cassandra, Cosmos DB, LiteDB, Memcached, DuckDB, ClickHouse, Snowflake, BigQuery, Prometheus, InfluxDB, OpenTSDB, Neo4j, ArangoDB, JanusGraph, and Neptune now mark import/export as strong for the contract slice because object-view actions, browser manifests/planners, Rust manifests/planners, backup/restore or bounded export request shapes, and plan-only file/cloud execution caveats stay synchronized. Adapter-owned file/cloud execution and live restore/import validation remain the residual risk.
- **2026-06-03: Wave 17 secondary object-view parity.** Search, DynamoDB/Cassandra, Cosmos DB, LiteDB, Memcached, DuckDB, ClickHouse, Snowflake, BigQuery, Prometheus, InfluxDB, OpenTSDB, Neo4j, ArangoDB, JanusGraph, and Neptune now mark object views as strong for the contract slice because descriptor-backed workflows, focused descriptor tests, workspace routing, posture panels, and guarded action strips stay synchronized. Live payload depth, plugin/capability-specific screens, cloud permission details, and fixture-backed UI validation remain the residual risk.
- **2026-06-03: Blank profile fail-closed pass.** Browser-preview MySQL/MariaDB, CockroachDB, Oracle, InfluxDB, and graph helpers now avoid invented database/container/bucket/graph roots for blank profiles. Incomplete MySQL/PostgreSQL/Cockroach table scopes also resolve to no target instead of defaulting to demo tables such as `accounts`.
- **2026-06-03: MongoDB view result wording.** MongoDB view child nodes now use `view-results` and the visible label `Results Preview`; the old `sample-results` kind remains a compatibility alias for older saved/open state, not a new tree surface.
- **2026-06-03: MongoDB first native-completion slice.** MongoDB now marks IntelliSense, diagnostics/performance, and import/export as strong for the contract slice: aggregation completions include stage snippets, expression operators, and `$field.path` references; diagnostics expose profiler/currentOp/replica/shard/index signals as profile/metric payloads; collection import/export plans declare JSON, Extended JSON, NDJSON, CSV, and BSON file workflows with adapter-owned live-execution evidence gates.
- **2026-06-03: MongoDB guarded file workflow slice.** Desktop MongoDB collection import/export now executes guarded JSON, Extended JSON, NDJSON, CSV, and BSON file workflows with concrete path checks, overwrite guards, read-only/confirmation gates, parser/serializer tests, duplicate-key policy handling, and before/after import summaries.
- **2026-06-04: MongoDB native-complete graduation.** MongoDB now has adapter-driven fixture evidence for desktop collection export/import, duplicate-skip import metadata, and live document replacement/delete execution. The optional MongoDB validator checks seeded catalog volume, large-document export primitives, import/export command primitives, duplicate-key and validator failures, read-only permission-denied diagnostics, and management before/after primitives. MongoDB is native-complete for the scoped document workflow; Atlas-only surfaces, live GridFS file workflows, replica/shard fixture depth, and live admin execution stay optional.
- **2026-06-03: Preview payload copy cleanup.** Cosmos diagnostics, warehouse external-stage metadata, and search pipeline simulation plans no longer display demo sample paths or sample document payloads when no real user input exists.

## Recommended Completion Order

1. Follow the active order in [Native Datastore Completion Tracker](native-completion-tracker.md), continuing with LiteDB native-completion work.
2. Use MongoDB, PostgreSQL, CockroachDB, SQL Server, MySQL, MariaDB, Redis, SQLite, TimescaleDB, Valkey, Oracle, DynamoDB, Elasticsearch, OpenSearch, and DuckDB as the native-complete reference patterns for the next engine.
3. Add optional live validation tracks for search extensions and remaining wide-column engines: Elasticsearch/OpenSearch cloud/admin flows and Cassandra.
4. Add optional fixture/cloud validation for Wave 4 and Wave 5 engines while keeping default CI dependency-free.
5. Promote selected contract-only operation previews to guarded live execution once adapter-owned permission, cost, environment, rollback, and confirmation checks exist.

## Documentation Rule

When datastore features change, update these together:

- user-facing summary in `README.md`
- product feature details in `docs/features.md`
- implementation/reference status in this file
- active queue and per-engine progress in `docs/architecture/native-completion-tracker.md`
- adapter backlog or completeness source in `packages/shared-types/src/datastore-roadmap.ts` or `packages/shared-types/src/datastore-completeness.ts` when the actual capability changes
