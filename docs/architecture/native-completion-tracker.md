# Native Datastore Completion Tracker

Last updated: 2026-06-07

This is the working tracker for moving DataPad++ from **contract-complete native UX** to **native-complete** datastore support, one engine at a time.

The source of truth for scoring and per-criterion status is `packages/shared-types/src/datastore-completeness.ts`. This document is the human-readable execution board: it records the chosen order, the current active datastore, the known native gaps, and the acceptance gate for graduating an engine.

## Status Model

- **Queued**: contract-complete, but native gaps remain.
- **In progress**: the current datastore being completed.
- **Blocked**: implementation cannot continue without a driver, fixture, credential model, or product decision.
- **Native-complete**: all 10 completion criteria are `strong` or `native`, docs and tests are updated, and residual risk no longer contradicts the claim.

For cloud or managed services, native-complete may still include optional live/cloud validation notes, but the default claim must not imply untested production credentials or destructive execution.

## Active Completion Run

| Field | Value |
| --- | --- |
| Active datastore | TimescaleDB |
| Current claim | Contract-complete, usable |
| Target claim | Native-complete scoped TimescaleDB datastore |
| Current score | 3.35 / 5 |
| Incomplete native criteria | TimescaleDB still needs deeper hypertable/chunk/compression/retention/continuous-aggregate/job object-view dashboards, guarded policy/job workflow execution boundaries, import/export/backup polish, and optional live fixture evidence beyond the PostgreSQL-wire base. |
| Remaining native-complete blocker | TimescaleDB still needs live Timescale metadata depth, rendered time-bucket/compression/job dashboards, before/after row-edit evidence polish, and guarded compression/retention/continuous-aggregate/import-export execution boundaries. |
| Next completion goal | Complete the TimescaleDB object-view/dashboard slice for time buckets, chunk sizing, compression coverage, continuous aggregate freshness, and job history without overclaiming live policy/admin execution. |

## Native-Complete Acceptance Gate

Every datastore must pass this gate before its tracker status can move to native-complete:

1. All 10 criteria are `strong` or `native` in `packages/shared-types/src/datastore-completeness.ts`.
2. The engine summary and residual-risk wording do not overclaim live/cloud/destructive validation.
3. User-facing docs are updated in `README.md`, `docs/features.md`, and `docs/architecture/datastore-readiness.md` when behavior changes.
4. This tracker is updated with the completed work and the next active datastore.
5. Deterministic default validation passes: `npm run check:all`, `npm run release:test`, and `npm run ci:workflow:test`.
6. `npm audit --omit=dev` is run and any remaining advisory is explicitly documented.
7. Optional live/fixture/cloud tests are added or documented when native-complete depends on external services.

## Completion Order

| Order | Datastore | Tracker status | Native gaps to close |
| --- | --- | --- | --- |
| 1 | MongoDB | Native-complete | Scoped document workflow is native-complete; Atlas-only surfaces, live GridFS file workflows, replica/shard fixture depth, and live admin execution remain optional extensions outside the release claim. |
| 2 | Redis | Native-complete | Scoped Redis and Redis Stack key workflow is native-complete; vector-set live fixture validation is an optional `--require-vector` extension for VADD-capable images, and cluster/Sentinel fixture evidence stays outside the release claim. |
| 3 | Valkey | Native-complete | Core single-node Valkey is native-complete; cluster/Sentinel and module-backed parity remain optional extensions gated outside the release claim unless live Valkey-compatible evidence is added. |
| 4 | SQLite | Native-complete | Scoped local-file SQL workflow is native-complete: local open/create, SQLite tree/query/IntelliSense/object views, live primary-key row edits, rendered explain payloads, guarded integrity/maintenance previews, and live guarded backup/export/import file workflows; restore execution, encrypted-provider builds, and deeper trigger/index authoring remain optional extensions outside the release claim. |
| 5 | PostgreSQL | Native-complete | Scoped PostgreSQL workflow is native-complete; full pg_dump/pg_restore execution, managed provider IAM/proxy/certificate combinations, generated/custom-type stress fixtures, richer privilege editors, and restore execution remain optional extensions outside the release claim. |
| 6 | SQL Server / Azure SQL | Native-complete | Scoped SQL Server/Azure SQL workflow is native-complete; native .bak BACKUP/RESTORE, bcp/sqlcmd bulk workflows, identity insert, and broader live maintenance/admin execution remain optional extensions outside the release claim. |
| 7 | MySQL | Native-complete | Scoped MySQL workflow is native-complete; cleartext/IAM auth, selected live admin execution, LOAD DATA INFILE, mysqlpump/mysqldump parity, richer grant editing, replication-channel depth, and full restore execution remain optional extensions outside the release claim. |
| 8 | MariaDB | Native-complete | Scoped MariaDB workflow is native-complete; selected live admin execution, LOAD DATA INFILE, mariadb-dump/mysql dump parity, richer role/grant editing, and full restore execution remain optional extensions outside the release claim. |
| 9 | CockroachDB | Native-complete | Scoped distributed SQL workflow is native-complete; live Cockroach Cloud/capability probing, rendered EXPLAIN ANALYZE DEBUG dashboards, job-control execution, zone changes, and live destructive/data-movement execution remain optional extensions outside the release claim. |
| 10 | TimescaleDB | In progress | Dashboard/object-view depth, import/export/backup polish, optional live fixture evidence, and guarded live compression, retention, continuous aggregate, and job policy execution. |
| 11 | Oracle | Queued | SQL-family gaps plus live driver path, live row editing, Oracle fixture tests, dictionary and SQL Monitor disabled reasons. |
| 12 | DynamoDB | Queued | PartiQL, richer expression helpers, and live SDK/SigV4 capacity/cost feedback. |
| 13 | Elasticsearch | Queued | Aggregation builder polish plus ES|QL where available. |
| 14 | OpenSearch | Queued | Aggregation builder polish plus OpenSearch SQL, ISM, plugin-specific query and diagnostics paths. |
| 15 | DuckDB | Queued | Safe local OLAP mutation/admin execution, import/export, extension, and file workflows. |
| 16 | LiteDB | Queued | Live query executor and live document/collection CRUD with file-lock and encryption safety. |
| 17 | Memcached | Queued | Live binary/text protocol routing, CAS/TTL-safe known-key edits, multi-node safety. |
| 18 | Cosmos DB | Queued | Non-SQL API builders and live document CRUD with partition-key and ETag safety. |
| 19 | ClickHouse | Queued | Safe table/admin execution, live import/export, maintenance, cluster metrics, and native protocol depth. |
| 20 | Snowflake | Queued | Safe warehouse/table operations, OAuth/programmatic auth, live history/utilization, import/export, task/stream/share management. |
| 21 | BigQuery | Queued | Safe table/job operations, ADC/service account auth, INFORMATION_SCHEMA metrics, IAM execution, scheduled queries, export/import jobs. |
| 22 | Cassandra | Queued | CQL query depth, live primary-key-safe row editing, live/fixture CQL driver tests. |
| 23 | Prometheus | Queued | PromQL builder depth, target/rule/alert/TSDB live views, range chart polish, optional live profile/cardinality execution. |
| 24 | InfluxDB | Queued | Live retention/delete/import workflows, token/version validation, task and retention management views. |
| 25 | OpenTSDB | Queued | Metric/tag query builder depth, UID/tree management execution, stats dashboards, HBase/backend guidance, API import/export. |
| 26 | Neo4j | Queued | Live Bolt/Cypher depth, graph visualization, rendered operator profiles, schema/security execution, safe graph mutations. |
| 27 | ArangoDB | Queued | Deeper live HTTP/AQL execution, collection/graph/index views, rendered explain/profile, Foxx/security execution. |
| 28 | JanusGraph | Queued | Live Gremlin depth, schema/index/property views, backend health, reindex execution, backend/index-service fixtures. |
| 29 | Neptune | Queued | Gremlin/openCypher/SPARQL mode switching, live IAM/SigV4, loader jobs, rendered explain/profile, CloudWatch dashboards. |

## Completed Native-Complete Engines

| Datastore | Scope | Completed |
| --- | --- | --- |
| MongoDB | Scoped document workflow: connection/tree/query/IntelliSense/object views, live guarded insert/replace/delete and field edits, guarded collection file import/export, adapter-driven fixture evidence, and optional permission/management fixture validation; Atlas/GridFS/replica-shard/live-admin extensions remain outside this claim. | 2026-06-04 |
| Redis | Scoped Redis and Redis Stack key workflow: key browser/console, command-aware IntelliSense, stream consumer-group drilldowns, Redis Stack module object views, guarded core/member, stream, RedisJSON, RedisTimeSeries, and vector-set edits, guarded key import/export for core/JSON/TimeSeries/vector/module snapshots, and optional fixture validation; vector-set fixture evidence is image-dependent via `--require-vector`, and cluster/Sentinel extensions remain outside this claim. | 2026-06-04 |
| PostgreSQL | Scoped SQL workflow: typed native connection/profile options, live SQL, primary-key row edits with before/after evidence, PostgreSQL-aware IntelliSense, extension/security/diagnostics object views, guarded routine/session/role/extension operations, rendered explain/profile dashboards, guarded desktop table import/export, bounded logical backup packages, and optional fixture validation; full pg_dump/pg_restore execution, managed-provider credential/proxy/certificate combinations, richer privilege editors, generated/custom-type stress fixtures, and restore execution remain outside this claim. | 2026-06-05 |
| SQL Server / Azure SQL | Scoped SQL Server/Azure SQL workflow: typed SQL Server/Azure SQL profiles, live TDS SQL, primary-key row edits, SQL Server IntelliSense/query helpers, SSMS-style tree and object views, SHOWPLAN_TEXT/XML Showplan renderers, Query Store, Extended Events, Agent, security/storage/runtime DMV payloads, guarded statistics/index/Query Store previews, guarded desktop CSV/JSON/NDJSON table import/export, bounded JSON/SQL logical backup package execution, and restore-package validation; native .bak BACKUP/RESTORE, bcp/sqlcmd bulk workflows, identity insert, and broader live maintenance/admin execution remain outside this claim. | 2026-06-06 |
| MySQL | Scoped MySQL workflow: typed native connection/profile options, live SQL, primary-key row edits, MySQL query helpers and IntelliSense, Workbench-style trees, storage/index/security/session/status/performance_schema/optimizer/InnoDB/replication object views, guarded management previews, guarded desktop CSV/JSON/NDJSON table import/export, bounded logical backup packages, and restore-package validation; cleartext/IAM auth, selected live admin execution, LOAD DATA INFILE, mysqlpump/mysqldump parity, richer grant editing, replication-channel depth, and full restore execution remain outside this claim. | 2026-06-06 |
| MariaDB | Scoped MariaDB workflow: typed MariaDB connection/profile options, live SQL, primary-key row edits, MariaDB query helpers and IntelliSense, MariaDB-aware Workbench-style trees, native role-mapping/server-variable/storage-engine/ANALYZE object views, role-aware security and Aria diagnostics, preview-first routine/event/admin/user workflows, guarded desktop CSV/JSON/NDJSON table import/export, bounded logical backup packages, and restore-package validation; selected live admin execution, LOAD DATA INFILE, mariadb-dump/mysql dump parity, richer role/grant editing, and full restore execution remain outside this claim. | 2026-06-06 |
| SQLite | Scoped local-file SQL workflow: local file open/create, SQLite-native tree/query/IntelliSense/object views, live guarded primary-key row edits, rendered explain/bytecode payloads, guarded integrity and maintenance previews, and live desktop VACUUM INTO backup plus CSV/JSON/NDJSON table/view export and table import workflows; restore execution, encrypted-provider builds, and deeper trigger/index authoring remain outside this claim. | 2026-06-04 |
| Valkey | Core single-node Redis-compatible workflow; optional cluster/Sentinel and module-backed parity are gated extensions outside this claim. | 2026-06-04 |

## Progress Log

- **2026-06-03:** Created this tracker from the current all-engine native gap audit. MongoDB is the first active datastore because it is closest to native-complete and should become the reference pattern for later engines.
- **2026-06-03:** Completed the first MongoDB slice: aggregation-stage IntelliSense now includes pipeline stages, expression operators, snippets, and `$field.path` references; diagnostics now expose profiler/currentOp/replica/shard/index signals as profile/metric payloads; collection import/export plans now declare JSON, Extended JSON, NDJSON, CSV, and BSON file workflows with an explicit adapter-owned live-execution evidence gate.
- **2026-06-03:** Completed the MongoDB guarded file workflow slice: desktop collection import/export now has a live executor for JSON, Extended JSON, NDJSON, CSV, and BSON with concrete path checks, overwrite guards, read-only/confirmation gates, parser/serializer tests, duplicate-key skip/stop policy, and import before/after summaries. MongoDB remains in progress until optional live fixture evidence and management/permission diagnostics are deepened.
- **2026-06-04:** Graduated MongoDB to native-complete for the scoped document workflow. The optional MongoDB fixture validator now checks seeded catalog volume, large-document export primitives, collection import/export command primitives, duplicate-key and validator failures, read-only permission-denied diagnostics, and management before/after primitives; the gated Rust fixture roundtrip now executes the actual desktop collection export/import workflow plus document replacement and delete. Atlas-only surfaces, live GridFS file workflows, replica/shard fixture depth, and live admin execution stay optional extensions outside the claim. Active completion moves to Redis.
- **2026-06-04:** Started Redis as the active reference datastore. Redis/Valkey IntelliSense now has a dedicated provider with read command syntax, key-position detection, namespace prefixes, subcommand completions, SCAN option/value completions, range/count/path literals, INFO sections, and live command metadata fallback. Redis remains in progress for module-backed workflows.
- **2026-06-04:** Completed the Redis guarded core key file workflow slice. Desktop Redis key export/import now supports JSON and NDJSON for strings, hashes, lists, sets, sorted sets, and streams with concrete path checks, overwrite/read-only/confirmation guards, create-only/replace/validate modes, TTL preservation, serializer/parser tests, and before/after metadata summaries. Valkey kept the Redis-compatible planner contract at this point until the later live key-file promotion separated core Redis-compatible types from Redis Stack-only formats. Redis remained in progress for module-backed workflows.
- **2026-06-04:** Completed the Redis object-view depth slice for core/admin payloads. Pub/Sub channel/pattern/subscriber leaf descriptors now stay Redis-specific, diagnostics render native `SLOWLOG GET`, `LATENCY LATEST`, `MEMORY STATS`, and `CLIENT LIST` replies, and `FUNCTION LIST` map-shaped replies normalize into function-library rows. Redis object views are now strong in the shared matrix.
- **2026-06-04:** Completed the Redis live COMMAND INFO ingestion slice. Redis console `COMMAND` and `COMMAND INFO` results now emit normalized command metadata tables and JSON `commandMetadata`; the IntelliSense catalog ingests that metadata from recent Redis/Valkey results, filters write commands out of completions, and uses live key-position hints for read commands. Redis remains in progress for module-backed workflows and optional fixture evidence.
- **2026-06-04:** Completed the Redis stream consumer-group drilldown slice. Stream keys now expand into Overview, Recent Entries, Consumer Groups, group detail, Consumers, and Pending Entries nodes backed by read-only `XINFO`, `XRANGE`, and `XPENDING` payloads, with encoded node IDs for colon-heavy stream keys and focused normalizer/sidebar tests.
- **2026-06-04:** Completed the Redis Stack module object-view slice. Redis module folders and module key inspections now render native panels for RedisJSON, RedisTimeSeries, RedisBloom, RediSearch, and vector sets using optional read-only probes such as `JSON.TYPE`, `TS.INFO`, `BF.INFO`, `FT._LIST`, `FT.INFO`, `VINFO`, `VCARD`, and `VDIM`, while remaining module import/export serializers stay explicitly gated until guarded workflows land.
- **2026-06-04:** Completed the Redis Stack module command-hint slice. Redis IntelliSense now has read-oriented hints and argument targets for RedisJSON, RedisTimeSeries, RedisBloom, RediSearch, and vector-set commands, module explorer nodes feed key/index completions, and Valkey hides Redis Stack static hints unless live command metadata proves support.
- **2026-06-04:** Completed the RedisJSON key file workflow slice. Desktop Redis key export/import now supports whole-document RedisJSON values through the guarded JSON/NDJSON workflow using `JSON.GET` and `JSON.SET key $`, with parser/serializer tests; Valkey plus the then-remaining module serializers stayed fixture-gated until later slices.
- **2026-06-04:** Completed the guarded RedisJSON path-edit slice. Redis key detail JSON trees now build explicit `json-set-path` / `json-delete-path` requests, desktop execution runs `JSON.SET` / `JSON.DEL` only for concrete Redis JSON keys after read-only/type/confirmation guardrails pass, and Valkey hides RedisJSON edit scopes.
- **2026-06-04:** Completed the guarded Redis stream-entry edit slice. Redis and Valkey now expose `stream-add-entry` / `stream-delete-entry` scopes, planners generate `XADD` / `XDEL` previews, desktop execution checks for an existing stream key before running, and key inspection samples surface entry-level rows with guarded delete actions.
- **2026-06-04:** Completed the guarded RedisTimeSeries sample-edit slice. Redis now exposes `timeseries-add-sample` / `timeseries-delete-sample` scopes, planners generate `TS.ADD` / `TS.DEL` previews, desktop execution checks for an existing TimeSeries key before running, and key inspection samples surface timestamp rows with guarded delete actions. Valkey keeps RedisTimeSeries edits hidden.
- **2026-06-04:** Completed the guarded Redis vector-set edit slice. Redis now exposes `vector-add-member`, `vector-remove-member`, and `vector-set-attributes` scopes. Browser and desktop planners emit `VADD`/`VREM`/`VSETATTR` previews, desktop execution checks for an existing vector-set key before running, key inspection samples show vector elements with guarded delete actions, and Valkey keeps vector edit scopes hidden.
- **2026-06-04:** Completed the Redis module key file serializer slice. Desktop Redis key export/import now supports RedisTimeSeries sample files via `TS.RANGE`/`TS.ADD`, vector-set element files via `VRANDMEMBER`/`VEMB`/`VGETATTR`/`VADD`, and Redis DUMP/RESTORE base64 snapshot envelopes for Bloom, Cuckoo, CMS, TopK, and t-digest module values. Redis remains in progress only for optional fixture/live evidence before a strict native-complete claim.
- **2026-06-04:** Added the opt-in Redis fixture evidence harness. The Docker fixture seed now creates Redis/Valkey stream consumer-group state, Redis Stack JSON/TimeSeries/probabilistic module data, and vector-set data when the selected Redis Stack image supports vector commands. `npm run fixtures:validate:redis -- --require-stack --require-valkey` validates the optional live evidence path outside default CI.
- **2026-06-04:** Ran the Redis fixture evidence harness after starting `redis-stack` and `cache` profiles and seeding all running fixtures. Validation passed for core Redis keys, Redis stream consumer groups, Redis Stack JSON, TimeSeries, Bloom, Cuckoo, CMS, TopK, t-digest, DUMP/RESTORE module snapshot clones, Valkey keys, and Valkey stream consumer groups. The current Redis Stack image reported no `VADD`, so vector-set live fixture evidence remains image-dependent rather than failed.
- **2026-06-04:** Completed the Valkey live key-file promotion slice. Valkey key import/export operation manifests now advertise guarded desktop live execution for strings, hashes, lists, sets, sorted sets, and streams while Redis Stack module serializers remain hidden/plan-only; the browser planner pins the same core-only contract, and the Redis fixture validator now checks Valkey export/import command primitives plus TTL behavior.
- **2026-06-04:** Completed the Valkey tree/menu copy and disabled-reason slice. Shared TypeScript and Rust tree manifests, sidebar fallbacks, browser preview explorer/structure responses, key-value delete copy, and Redis module disabled-action payloads now use Valkey-specific wording and avoid Redis Stack/vector-only claims unless compatible live evidence exists.
- **2026-06-04:** Completed the Valkey permission and large key-file evidence slice. The optional Redis fixture validator now creates a temporary read-only Valkey ACL user to prove write-denial behavior, validates that denied writes leave seed data untouched, and creates/removes large list and stream keys to prove the core JSON/NDJSON key-file command primitives at larger payload sizes.
- **2026-06-04:** Graduated Valkey to native-complete for the core single-node Valkey claim. Cluster/Sentinel and Valkey-compatible module parity are now explicit optional extensions outside the release claim, while Redis Stack/vector-only surfaces stay hidden unless live metadata proves support. Active completion remained on Redis to settle the Redis Stack boundary.
- **2026-06-04:** Graduated Redis to native-complete for the scoped Redis and Redis Stack key workflow. The Redis Stack vector-set fixture check is now explicitly optional through `--require-vector` because the current fixture image does not expose `VADD`; core Redis, stream groups, RedisJSON, RedisTimeSeries, probabilistic modules, DUMP/RESTORE module snapshots, and Valkey evidence remain covered by the optional validator. Active completion moves to SQLite.
- **2026-06-04:** Started SQLite as the active local-file SQL target. The audit narrowed the remaining native-complete gap to scoped file workflows and documentation/test alignment because SQLite already had local file connection flow, native trees, scoped query paths, object views, rendered explain payloads, guarded row edits, and maintenance previews.
- **2026-06-04:** Graduated SQLite to native-complete for the scoped local-file SQL workflow. Desktop SQLite now executes guarded `VACUUM INTO` backups, bounded table/view export to CSV/JSON/NDJSON, and CSV/JSON/NDJSON table import into explicit existing targets after read-only, absolute-path, overwrite, row-limit, validation-only, and confirmation checks. Restore execution, encrypted-provider builds, and deeper trigger/index authoring remain optional extensions outside the claim. Active completion moves to PostgreSQL.
- **2026-06-05:** Completed the first PostgreSQL diagnostics/profile slice. Desktop diagnostics now emit live `pg_stat_activity` session/wait/blocking profiles, `pg_locks` lock posture profiles, `pg_stat_user_tables` relation/vacuum/index-scan profiles, optional `pg_stat_statements` top-query profiles when the extension is visible, and matching metrics/query-history payloads. Browser and Rust planners now generate an explicit guarded `EXPLAIN (ANALYZE, BUFFERS, VERBOSE, FORMAT JSON)` profile request instead of a generic `explain analyze` string. PostgreSQL remains in progress for role/grant depth, extension-aware views, before/after row-edit evidence, rendered profile dashboards, and guarded import/export/backup execution.
- **2026-06-05:** Completed the PostgreSQL role/grant and extension-aware object-view slice. Live and browser explorer payloads now include extension folders, extension update hints, extension-owned objects, role memberships, default privileges, and normalized schema/table/routine/sequence permissions. Object views render those security and extension panels, and guarded role grant/revoke plus extension update/drop operation previews now have browser and Rust request-shape coverage. At that point, PostgreSQL still needed editor polish, before/after row-edit evidence, rendered EXPLAIN ANALYZE dashboards, and guarded import/export/backup execution.
- **2026-06-05:** Completed the PostgreSQL row-edit evidence slice. Desktop PostgreSQL-family row edits now use mutation statements with `RETURNING *`; update/delete paths prefetch the primary-key target with a bounded `SELECT`, and execution metadata now includes `rowEvidence.before`, `rowEvidence.after`, the primary-key predicate, and generated statements for review. At that point, PostgreSQL still needed editor polish, rendered EXPLAIN ANALYZE dashboards, and guarded import/export/backup execution.
- **2026-06-05:** Completed the PostgreSQL guarded file-workflow slice. Desktop PostgreSQL now executes bounded table/view export to CSV/JSON/NDJSON, CSV/JSON/NDJSON import into explicit existing tables with target-column and type-cast validation plus validation-only mode, and bounded JSON/SQL logical backup packages after concrete-path, overwrite, read-only, row/table-limit, and confirmation guardrails. Browser and Rust planners now emit matching `postgresql.table.export`, `postgresql.table.import`, and `postgresql.database.backup` workflow contracts. At that point, PostgreSQL still needed editor polish, rendered EXPLAIN ANALYZE dashboards, optional fixture evidence, and the full pg_dump/restore boundary.
- **2026-06-05:** Completed the PostgreSQL rendered profile-dashboard slice. PostgreSQL now accepts a first-class `profile` execution mode for SELECT/WITH/VALUES statements, wraps it with adapter-owned `EXPLAIN (ANALYZE, BUFFERS, VERBOSE, FORMAT JSON)`, renders normalized operator stages through the profile renderer, keeps plan/table/raw fallbacks for JSON auditability, and promotes the guarded `postgresql.query.profile` operation to live after confirmation. At that point, PostgreSQL still needed editor polish, optional fixture evidence, session/action guardrails, and the full pg_dump/restore boundary.
- **2026-06-05:** Completed the PostgreSQL IntelliSense/query-helper slice. The SQL completion provider now layers PostgreSQL-native catalog helpers, `pg_catalog`/`information_schema` objects, dialect functions/keywords, routine call and definition snippets from cached explorer metadata, profile/session/lock/routine/extension snippets, and safe PostgreSQL identifier quoting on top of the shared SQL metadata provider. At that point, PostgreSQL remained in progress for connection/profile option depth, dedicated parameterized routine execution workflows, optional fixture evidence, session/action guardrails, and the full pg_dump/restore boundary.
- **2026-06-05:** Completed the PostgreSQL connection/profile option slice. Shared profiles now carry typed PostgreSQL options for TCP, Unix socket, Cloud SQL proxy, managed metadata, application name, search path, target session attributes, TLS certificate paths, and connect/statement/lock/idle timeouts. The right drawer exposes those fields for PostgreSQL/TimescaleDB, Rust interpolates them, connection tests use the configured timeout, and the PostgreSQL DSN builder emits encoded native query parameters without storing secret values. PostgreSQL remains in progress for parameterized routine execution workflows, optional fixture evidence, session/action guardrails, and the full pg_dump/restore boundary.
- **2026-06-05:** Completed the PostgreSQL parameterized routine execution slice. Function and procedure object views now expose a guarded Run action that carries routine kind, signature, return metadata, and parameterized bindings into browser and Rust operation planners. The PostgreSQL query-surface criterion is now strong; PostgreSQL remains contract-complete, not native-complete, until optional fixture evidence, session/action guardrails, and the full pg_dump/restore boundary are settled.
- **2026-06-05:** Completed the PostgreSQL session/action guardrail slice. Diagnostics and session views now expose guarded Cancel and Terminate actions that produce `pg_cancel_backend` and `pg_terminate_backend` previews with PID targeting, current-backend protection, permission warnings, confirmation requirements, and destructive transaction-impact wording. PostgreSQL remains contract-complete, not native-complete, until optional fixture evidence and the full pg_dump/restore boundary are settled or explicitly scoped out.
- **2026-06-05:** Graduated PostgreSQL to native-complete for the scoped SQL workflow. The optional PostgreSQL fixture validator now checks seeded relational volume, catalog/security/extension visibility, diagnostics and lock/session primitives, rendered profile/routine primitives, row-edit before/after evidence, table import/export primitives, bounded logical backup evidence, and permission-denied writes through a temporary read-only role; the local fixture run passed, with `pg_stat_statements` top-query evidence noted as optional because the default fixture does not install the extension. Full `pg_dump`/`pg_restore` execution, managed-provider IAM/proxy/certificate combinations, generated/custom-type stress fixtures, richer privilege editors, and restore execution stay optional extensions outside the claim. Active completion moves to SQL Server/Azure SQL.
- **2026-06-05:** Completed the first SQL Server/Azure SQL native-completion slice. Browser and Rust operation planners now generate a guarded `SHOWPLAN_XML` profile request for `sqlserver.query.profile`, while `query.explain` keeps `SHOWPLAN_TEXT`. The desktop SQL Server adapter now recognizes XML Showplan result sets, extracts statement estimates and relational operators into shared plan-renderer lines, operator tables, structured metadata, and raw XML audit samples. At this point, SQL Server still needed Query Store, Extended Events, Agent/security/storage payload depth, Azure auth disabled reasons, runtime statistics depth, and guarded live import/export or backup execution boundaries.
- **2026-06-05:** Completed the SQL Server Query Store payload-depth slice. SQL Server object inspections now collect Query Store status/options, top-query runtime rows with SQL text, duration, CPU, logical/physical read, execution, plan-state, force-failure, compatibility, and engine-version signals, forced-plan rows, and recent-vs-prior regression hints. The object-view workload panel now renders Query Store state, top-query, forced-plan, and regression cards with focused row chips instead of relying on raw JSON. SQL Server remains in progress for Extended Events, deeper Agent/security/storage payloads, Azure auth disabled reasons, runtime statistics depth, and guarded live import/export or backup execution boundaries.
- **2026-06-05:** Completed the SQL Server Extended Events payload-depth slice. SQL Server object inspections now collect database-scoped and server-scoped Extended Events sessions, captured event definitions, targets, running/stopped state, startup/retention/memory/causality settings, event and target counts, target data availability, and permission/empty-state warnings. The SQL Server object-view insights panel now renders Extended Events session/event/target cards and focused target chips, while shared relational sections expose detailed rows for `extended-events` and `xevent-profiler` views. SQL Server remains in progress for deeper Agent/security/storage payloads, Azure auth disabled reasons, runtime statistics depth, and guarded live import/export or backup execution boundaries.
- **2026-06-05:** Completed the SQL Server Agent payload-depth slice. SQL Server object inspections now collect `msdb`-backed Agent jobs, schedules, alerts, operators, proxies, optional Agent service status from `sys.dm_server_services`, last/next run signals, enabled/scheduled state, owners/categories, notification channels, proxy credentials, and Azure/restricted-login empty-state warnings. The Agent object-view panel and shared relational sections now render Agent service, jobs, schedules, alerts, operators, and proxies with focused workflow chips. SQL Server remains in progress for deeper security/storage payloads, Azure auth disabled reasons, runtime statistics depth, and guarded live import/export or backup execution boundaries.
- **2026-06-05:** Completed the SQL Server security/storage payload-depth slice. SQL Server security inspections now collect users, roles, role memberships, schemas, database permissions with securable/grantable context, certificates, symmetric/asymmetric keys, database-scoped credentials, and database audit specifications. Storage inspections now collect files, filegroups, partition schemes, partition functions, partition boundaries, and allocation-unit totals. The SQL Server posture panel, shared detail sections, workflow chips, descriptors, explorer routing, and query templates now expose those surfaces without falling back to generic object views. SQL Server remains in progress for Azure auth disabled reasons, runtime statistics depth, and guarded live import/export or backup execution boundaries.
- **2026-06-06:** Completed the SQL Server Azure auth disabled-reason slice. Shared runtime validation now preserves SQL Server/Azure SQL connection option metadata, the right drawer shows per-mode plan-only reasons for Windows Integrated, Microsoft Entra password/integrated/interactive, managed identity, service principal, and certificate modes, and the Rust live connection gate emits matching mode-specific disabled reasons instead of a generic auth failure. SQL Server remains in progress for runtime statistics profile depth and guarded live import/export or backup execution boundaries.
- **2026-06-06:** Completed the SQL Server runtime statistics profile-depth slice. SQL Server performance object views now collect and render cached statement runtime stats from `sys.dm_exec_query_stats`, active request/session rows from `sys.dm_exec_requests`, wait pressure, file I/O stalls, memory grants, active transaction log usage, and missing-index impact, with browser/Rust preview requests pointing at the same DMV surfaces. SQL Server remains in progress for guarded live import/export or backup execution boundaries.
- **2026-06-06:** Completed the SQL Server guarded file-workflow and native-complete graduation slice. SQL Server import/export and backup/restore manifests now advertise scoped guarded live support, browser and Rust planners emit matching workflow contracts, and the desktop adapter executes bounded table export/import plus JSON/SQL logical backup packages with concrete path, overwrite, read-only, row/table-limit, column-validation, confirmation, and restore-package validation guardrails. SQL Server/Azure SQL is native-complete for the scoped claim; native .bak BACKUP/RESTORE, bcp/sqlcmd bulk workflows, identity insert, and broader live maintenance/admin execution remain optional extensions outside the claim. Active completion moves to MySQL.
- **2026-06-06:** Completed the first MySQL native-completion slice: live desktop diagnostics now collect `SHOW GLOBAL STATUS`, processlist/session waits, performance_schema statement digests, table/index I/O waits, metadata locks, InnoDB status counters, and optimizer trace availability; browser previews and object views render the same Workbench-style performance_schema/status/optimizer surfaces. The follow-up file-workflow and management/action slices below closed the scoped import/export, logical-backup, and guarded-operation boundaries.
- **2026-06-06:** Completed the MySQL guarded file-workflow slice. MySQL import/export and backup/restore manifests now advertise scoped guarded live support, browser and Rust planners emit matching `mysql.table.export`, `mysql.table.import`, and `mysql.database.backup` workflow contracts, and the desktop adapter executes bounded CSV/JSON/NDJSON table export/import, validation-only imports, insertable-column validation for auto/generated columns, bounded JSON/SQL logical backup packages, and restore-package validation after concrete-path, overwrite, read-only, row/table-limit, and confirmation guardrails. Full `LOAD DATA INFILE`, mysqlpump/mysqldump parity, generated-column mapping controls, and restore execution remain optional extensions outside the current claim.
- **2026-06-06:** Completed the MySQL native management/action polish slice. Browser and Rust planners now emit structured workflow contracts for `CHECK`, `ANALYZE`, `OPTIMIZE`, and `REPAIR` table maintenance, parameter-aware routine execution previews, event enable/disable previews, security inspection, and user account lock/unlock previews. MySQL operation manifests now keep those admin actions explicit as preview-only with privilege, read-only, confirmation, scheduler/definer/account, lock-impact, disabled-reason, and residual-risk guardrails; scoped file workflows remain the live execution boundary.
- **2026-06-06:** Completed the MySQL connection/profile polish slice. Shared profiles now carry typed MySQL options for TCP, Unix socket, Cloud SQL socket, managed metadata, auth-mode metadata, SSL modes, certificate paths, charset, collation, time zone, statement cache capacity, and connect/command timeouts. The right drawer exposes those controls, browser validation normalizes and rejects unsafe option shapes, Rust interpolates profile values without secret leakage, connection tests route the configured timeout, seed fixtures carry MySQL option bags, and the SQLx DSN builder emits supported SSL/socket/session/cache parameters. Cleartext plugin and IAM token auth stay plan-only until live runtime support and fixture-backed permission evidence are added.
- **2026-06-06:** Completed the MySQL query-helper and IntelliSense polish slice. The shared SQL SELECT builder now emits MySQL backtick identifiers and numeric boolean literals, MySQL IntelliSense adds dialect keywords/functions, information_schema and performance_schema catalog helpers, routine call/definition snippets, EXPLAIN FORMAT=JSON, optimizer trace, processlist/wait, statement digest, metadata-lock, and routine inventory snippets, and the shared SQL alias parser now understands backticked MySQL object aliases.
- **2026-06-06:** Completed the MySQL object-view hardening and native-complete graduation slice. MySQL diagnostic leaves now use MySQL-native descriptors, performance_schema/status/optimizer payloads render detailed statement digest, table/index I/O, metadata-lock, optimizer-trace, and status-counter sections with focused workflow chips, diagnostics cards no longer treat status counters as storage metadata, and the shared matrix now marks MySQL object tree and object views strong. MySQL is native-complete for the scoped claim; cleartext/IAM auth, selected live admin execution, LOAD DATA INFILE, mysqlpump/mysqldump parity, richer grant editing, replication-channel depth, and full restore execution remain optional extensions outside the claim. Active completion moves to MariaDB.
- **2026-06-06:** Completed the first MariaDB native-completion slice. MariaDB now has explicit `EXPLAIN FORMAT=JSON` and guarded `ANALYZE FORMAT=JSON` previews, MariaDB-specific IntelliSense helpers for status/version/storage engines and roles, diagnostics contracts for version variables, storage engines, role mappings, Aria counters, and `ANALYZE FORMAT=JSON` profile samples, plus MariaDB-labeled object-view posture cards. The shared matrix now marks MariaDB query surface, IntelliSense, diagnostics/performance, and tests strong; connection flow, object tree, object-view polish, guarded operations, and import/export remained active at that point.
- **2026-06-06:** Completed the MariaDB connection/profile and tree polish slice. MariaDB connection profiles now carry managed-MariaDB mode, server flavor, SQL mode, default storage engine, charset/collation, LOCAL INFILE metadata, auth disabled reasons, validation, seed fixtures, and Rust interpolation. Shared TypeScript, Rust, browser-preview, and live explorer trees now expose MariaDB-aware role mappings, server variables, storage engines, and `ANALYZE FORMAT=JSON` diagnostics while hiding MySQL-only optimizer trace. The shared matrix now marks MariaDB connection flow and object tree strong; object-view polish, guarded operations, and import/export remained active at that point.
- **2026-06-06:** Completed the MariaDB object-view polish slice. MariaDB descriptors, relational section routing, browser-preview payloads, and object-view posture cards now render MariaDB role mappings, server variables, storage engines, `ANALYZE FORMAT=JSON` profile metadata, routines/events/security/diagnostics, and MariaDB-specific labels while hiding MySQL-only optimizer trace. The shared matrix marked MariaDB object views strong; guarded operations and import/export were the remaining active items at that point.
- **2026-06-06:** Graduated MariaDB to native-complete for the scoped SQL workflow. Browser and Rust planners now emit MariaDB-specific guarded routine/event/admin/user previews, role-aware security inspection, live guarded `mariadb.data.import-export` and `mariadb.data.backup-restore` workflow contracts, and the desktop adapter routes MariaDB CSV/JSON/NDJSON table import/export, bounded JSON/SQL logical backup packages, and restore-package validation through the MySQL-family guarded file executor. Selected live admin execution, `LOAD DATA INFILE`, mariadb-dump/mysql dump parity, richer role/grant editing, and full restore execution remain optional extensions outside the claim. Active completion moves to CockroachDB.
- **2026-06-06:** Completed the first CockroachDB distributed native-completion slice. The shared matrix now gives CockroachDB a dedicated profile and marks object tree, query surface, IntelliSense, object views, diagnostics/performance, safe editing, and tests strong. Browser preview now routes focused CockroachDB ranges, regions/localities, jobs, sessions, statements, transactions, locks, contention, statistics, grants, certificates, and zone-configuration nodes to CockroachDB-shaped payloads instead of generic schema fallbacks; IntelliSense now adds CockroachDB SHOW helpers, `crdb_internal` diagnostic objects, distributed explain/profile snippets, contention dashboards, and zone-configuration snippets. Remaining active criteria are connection flow, guarded operations, and import/export.
- **2026-06-06:** Completed the CockroachDB connection/profile and capability-hiding slice. Shared profiles now preserve CockroachDB deployment mode, Cockroach Cloud organization/cluster metadata, region/locality/version/build metadata, auth/TLS disabled reasons, and explicit capability toggles for jobs, ranges, regions, cluster status/settings, sessions, contention, roles/grants, certificates, zone configurations, and `EXPLAIN ANALYZE`. The right drawer exposes those controls, browser validation normalizes them, Rust profile interpolation preserves them without secret leakage, browser/Rust explorers hide profile-restricted surfaces, direct inspections report restricted warnings, and Cockroach operation manifests/object-view actions honor the same capability gates. Remaining active criteria are guarded operations and import/export.
- **2026-06-06:** Graduated CockroachDB to native-complete for the scoped distributed SQL workflow. Browser and Rust planners now emit Cockroach-owned guarded jobs, ranges, regions, sessions, contention, role/grant/default-privilege, zone-configuration, `IMPORT`, `EXPORT`, `BACKUP`, `RESTORE`, generic import/export, and generic backup/restore request builders with external-storage parameters, confirmation text, permission requirements, read-only/environment guard warnings, and scan/cost impact wording. Object-view action strips expose Cockroach import/export separately, profile capability gates still hide restricted native surfaces, and destructive/data-movement execution stays preview-first. Live Cockroach Cloud/capability probing, rendered EXPLAIN ANALYZE DEBUG dashboards, job-control execution, zone changes, and live destructive/data-movement execution remain optional extensions outside the scoped claim. Active completion moves to TimescaleDB.
- **2026-06-07:** Completed the TimescaleDB connection/profile and capability-hiding slice. Shared profiles now preserve Timescale deployment mode, project/service, region, extension schema/version, server version, license, policy disabled reasons, and explicit capability toggles for hypertables, chunks, compression, retention, continuous aggregates, jobs, Toolkit, `EXPLAIN ANALYZE`, and live policy execution. The right drawer exposes those controls, browser validation normalizes them, Rust interpolation preserves them without secret leakage, connection tests compare visible extension schema/version with profile expectations, browser inspections return restricted warnings for disabled Timescale catalog surfaces, object views render Timescale profile posture, and policy manifests use profile-specific disabled reasons while execution remains preview-first. Remaining active criteria are dashboard/object-view depth, import/export/backup polish, optional fixture evidence, and guarded live policy execution boundaries.

## Update Rule

When an engine moves forward:

- update its row in this tracker
- add a short progress-log entry with the date and wave/slice name
- update the shared completeness matrix if a criterion changes
- update the readiness plan and user-facing docs if behavior changes
- run the required validation gate and record any residual audit exception in the final work summary
